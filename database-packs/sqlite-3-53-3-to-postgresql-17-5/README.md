# SQLite 3.53.3 to PostgreSQL 17.5

Exact local disposable reference pack with typed canonical IR, real engine execution, row-level reconciliation, constraint and transaction negatives, query plans, backup, restore, and cleanup. Production writes, CDC, cutover, and independent certification remain `NOT_RUN`.
