# SQLite 3.53.3 to PostgreSQL 17.5

Exact local disposable reference pack with typed canonical IR, real engine execution, row-level reconciliation, constraint and transaction negatives, query plans, backup, restore, checkpointed backfill, offline delete-delta and cutover rehearsal, and cleanup. Production writes, online CDC, production cutover, and independent certification remain `NOT_RUN`.
