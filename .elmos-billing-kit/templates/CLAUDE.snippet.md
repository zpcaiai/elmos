## Elmos billing implementation rules

Use skills under `.claude/skills/` and `.elmos-billing-kit/`. Independently verify prior completion claims. Financial writes require integer amounts, balanced immutable ledger entries, idempotency, tenant isolation, audit, pre-side-effect budget caps, and executable evidence. Do not activate example prices or bypass blockers.
