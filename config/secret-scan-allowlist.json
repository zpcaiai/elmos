{
  "$comment": "Every entry needs a reason, an owner and an expiry. Expired entries stop suppressing automatically, so a suppression cannot outlive its justification. Add an entry only after confirming the finding is not a live credential; if it is one, rotate it and remove it from the tree instead.",
  "allowed": []
}
