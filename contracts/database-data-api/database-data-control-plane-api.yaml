openapi: 3.1.0
info:
  title: ELMOS ChinaDB SQL Preflight Control-Plane API
  version: 1.0.0
  description: Authenticated, tenant-bound proxy to the database-data worker. It may return bounded compatibility-mode SQL for an exact allowlisted request, but never claims vendor-native execution or certification.
servers:
  - url: http://localhost:8080
security:
  - bearerAuth: []
paths:
  /api/v1/database-data/sql-preflight/capabilities:
    get:
      operationId: getGovernedChinaDbSqlPreflightCapabilities
      description: Requires workspace:view and returns the exact fail-closed commercial planning snapshot.
      responses:
        "200":
          description: Snapshot with 13 target identities and 78 planned routes; compatibility-mode availability is not vendor-runtime evidence.
          content:
            application/json:
              schema:
                $ref: "../../schemas/batch31/chinadb-commercial-capabilities.schema.json"
        "401": { $ref: "#/components/responses/Problem" }
        "403": { $ref: "#/components/responses/Problem" }
        "502": { $ref: "#/components/responses/ChinaDbSqlFailure" }
        "503": { $ref: "#/components/responses/ChinaDbSqlFailure" }
  /api/v1/database-data/sql-preflight/assess:
    post:
      operationId: assessGovernedChinaDbSourceSql
      description: Requires translation:execute and derives tenant and actor from authenticated identity. Unsupported requests remain BLOCKED; an exact allowlisted request may return LOCAL_EMITTED compatibility SQL without vendor-runtime evidence.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "../../schemas/batch31/chinadb-sql-preflight-request.schema.json"
      responses:
        "200":
          description: Typed source AST, obligations, blockers, and optional compatibility-mode SQL; target execution and external checks remain NOT_RUN.
          content:
            application/json:
              schema:
                $ref: "../../schemas/batch31/chinadb-sql-preflight-result.schema.json"
        "400": { $ref: "#/components/responses/ChinaDbSqlFailure" }
        "401": { $ref: "#/components/responses/Problem" }
        "403": { $ref: "#/components/responses/Problem" }
        "409": { $ref: "#/components/responses/ChinaDbSqlFailure" }
        "413": { $ref: "#/components/responses/ChinaDbSqlFailure" }
        "502": { $ref: "#/components/responses/ChinaDbSqlFailure" }
        "503": { $ref: "#/components/responses/ChinaDbSqlFailure" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    ChinaDbSqlFailure:
      description: Sanitized fail-closed SQL-preflight response.
      content:
        application/json:
          schema:
            $ref: "./database-data-api.yaml#/components/schemas/ChinaDbSqlFailure"
    Problem:
      description: Authentication or authorization failed before SQL preflight execution.
