# Target contract tests

Required suites: web dispatch, request/response/error, bean graph, property
precedence, validation, filter/interceptor ordering, view/static resources and
lifecycle. Provider suites are additionally required when security, persistence,
transactions, messaging, cache or scheduling are detected. Execution is `NOT_RUN`.
