# Gap inventory

## Implemented static assets

- Exact directional source and target tuple.
- Source fingerprint contract for XML, annotation and servlet bootstrap paths.
- Framework Contract Model with explicit status for every Batch 30 behavior domain.
- Pinned deterministic OpenRewrite recipe composition and manual-obligation blockers.
- Traditional Spring MVC WAR development fixture.
- Fail-closed executable-WAR target emitter for the admitted XML/Servlet/JSP subset.
- Boot main and `SpringBootServletInitializer`, explicit Java MVC configuration,
  Tomcat Jasper, health-only Actuator and Maven repackage configuration.
- Content-addressed source map and XML/`web.xml` retirement receipts.
- Negative/holdout/representative corpus manifests.

## Completed exact local executable closure

- Exact Java 11/Maven 3.9.11 source `clean verify` and external Tomcat 9.0.120 startup.
- Source snapshot and FCM extraction before transformation.
- Pinned OpenRewrite plus the trusted Java exact-fixture materializer.
- Exact Java 21/Maven 3.9.11 target `clean verify`, package and WarLauncher startup.
- Actuator, preserved validation/error tests, JSON GET and admitted JSP byte comparison.
- Bounded source/target shutdown and outer content-addressed receipt binding.

## Remaining executable closure

- Verify DI bean graph and configuration precedence beyond the exercised fixture paths.
- Add exact profiles before exercising JSP tag libraries, security, persistence,
  transaction, messaging, cache or scheduler.
- Run independent negative, holdout and representative repositories.
- Run authorized customer, Rootless and external independent gates.

The exact fixture route is `PASSED_LOCAL`; the remaining items stay `NOT_RUN` and
the maximum pack status remains `experimental / NOT_CERTIFIED`.
