# Gap inventory

## Implemented static assets

- Exact directional source and target tuple.
- Source fingerprint contract for XML, annotation and servlet bootstrap paths.
- Framework Contract Model with explicit status for every Batch 30 behavior domain.
- Pinned deterministic OpenRewrite recipe composition and manual-obligation blockers.
- Traditional Spring MVC WAR development fixture.
- Fail-closed target profile, negative/holdout/representative corpus manifests.

## Required executable closure

- Run source baseline with exact Java 11 and Maven 3.9.11 in an approved container.
- Extract source runtime bean, filter, interceptor, property and lifecycle facts.
- Execute the pinned recipe and deterministic target scaffold materialization.
- Run target build/startup with exact Java 21 and Maven 3.9.11.
- Replay web, DI, configuration, validation, error and lifecycle contracts.
- Add exact profiles before exercising security, persistence, transaction, messaging,
  cache, scheduler or JSP/view workloads.
- Run independent negative, holdout and representative repositories.
- Run authorized customer, Rootless and external independent gates.

Until these steps produce immutable evidence, all execution states remain `NOT_RUN`
and the maximum status is `experimental / NOT_CERTIFIED`.
