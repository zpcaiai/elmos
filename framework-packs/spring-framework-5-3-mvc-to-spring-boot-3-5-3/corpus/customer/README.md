# Authorized customer corpus boundary

This directory is reserved for customer-owned, authorization-bound acceptance
inputs. No customer repository or data is checked in. Selection, hydration,
execution, retention, and cleanup remain `NOT_RUN` until an authorized customer
provides an exact content-addressed workload through the Batch 30 external
evidence workflow.

Files in this directory must never be used to tune recipes or weaken an oracle.
