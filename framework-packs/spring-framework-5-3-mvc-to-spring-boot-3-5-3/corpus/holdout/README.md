# Holdout corpus

No holdout repository has been selected or executed. A future holdout must be
physically separate, must not be used to tune the recipe, and should cover
programmatic servlet initialization, multiple XML contexts, custom handler
ordering, security, persistence/transactions, and an external container.

Status: `NOT_RUN`. Customer holdout: `NOT_RUN`.
