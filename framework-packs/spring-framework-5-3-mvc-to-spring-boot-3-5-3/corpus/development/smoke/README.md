# Development smoke corpus

Prepared obligations: exact Maven/Java tuple detection, WAR baseline build,
servlet-container startup, `/api/orders/42`, invalid POST, view rendering and
bounded shutdown. No command has been executed; status is `NOT_RUN`.
