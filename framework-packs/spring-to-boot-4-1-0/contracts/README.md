# Framework contract model

The contract is deliberately source-agnostic. The engine adds source traces
and exact observations to the run-scoped FCM before any target generation. The
pack does not treat a recipe diff as proof of web, security, data or runtime
equivalence.
