# ELMOS Java Control Plane Cloud Run production pack

This exact Batch 33 pack covers only the repository-owned Java Control Plane to
Google Cloud project `gen-lang-client-0684615336` (`nexus`), Cloud Run v2 in
`asia-east1`, then the Vercel `elmos` production origin binding.

Use `deploy/cloud-run/control-plane/README.md` for the operator workflow. The
pack is `experimental`; local tests do not prove provider apply, live identity,
rollback, cleanup, cost, drift, independent evidence, or certification.
