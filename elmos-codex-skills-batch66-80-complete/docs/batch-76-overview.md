# Batch 76 — Makefile CMake and Nginx Build and Proxy Pack

Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification

Skill range: **PG351–PG362** (12 Skills)

## Skills

- **PG351 — `b76-makefile-profile-detector`**: Inspect and model Makefile Profile Detector for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG352 — `b76-portable-makefile-generator`**: Generate runnable and maintainable Portable Makefile Generator for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG353 — `b76-make-incremental-dependency-manager`**: Coordinate and control Make Incremental Dependency Manager for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG354 — `b76-make-test-release-orchestrator`**: Coordinate and control Make Test Release Orchestrator for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG355 — `b76-cmake-profile-detector`**: Inspect and model CMake Profile Detector for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG356 — `b76-cmake-target-graph-generator`**: Generate runnable and maintainable CMake Target Graph Generator for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG357 — `b76-cmake-dependency-package-manager`**: Coordinate and control CMake Dependency Package Manager for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG358 — `b76-cmake-presets-crossbuild-manager`**: Coordinate and control CMake Presets Crossbuild Manager for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG359 — `b76-nginx-topology-profile-detector`**: Inspect and model Nginx Topology Profile Detector for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG360 — `b76-nginx-reverse-proxy-generator`**: Generate runnable and maintainable Nginx Reverse Proxy Generator for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG361 — `b76-nginx-tls-security-rate-limit`**: Implement and validate Nginx TLS Security Rate Limit for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.
- **PG362 — `b76-build-proxy-configuration-certifier`**: Execute and independently verify Build Proxy Configuration Certifier for Makefile and CMake build graphs plus Nginx reverse proxy, TLS, rate limiting, routing, and configuration certification, preserving ownership, compatibility, security, reproducibility, and evidence boundaries.

## Exit gate

- Installable Codex Skill layout validated.
- Runtime claims require exact external tool and environment evidence.
