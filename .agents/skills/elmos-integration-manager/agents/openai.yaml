interface:
  display_name: Integration Manager
  short_description: Run bounded Integration Manager repository capability
  default_prompt: Use $elmos-integration-manager with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
