interface:
  display_name: "elmos-reflection-dynamic-dispatch-semantics"
  short_description: "Characterize runtime-discovered members, proxy invocation, dynamic method lookup and reflection-v..."
  default_prompt: "Use $elmos-reflection-dynamic-dispatch-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-reflection-dynamic-dispatch-semantics"
  handler_id: "execute_elmos_reflection_dynamic_dispatch_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
