interface:
  display_name: "Compiler Grade Certification Gate"
  short_description: "Run compiler-grade-certification-gate with bounded commercial controls"
  default_prompt: "Use $compiler-grade-certification-gate through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
