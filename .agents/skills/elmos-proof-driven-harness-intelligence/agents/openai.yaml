interface:
  display_name: "Proof-Driven Harness Intelligence"
  short_description: "Route exact repository work through the typed PDHI v1 kernels."
  default_prompt: "Use $elmos-proof-driven-harness-intelligence through the exact PDHI v1 ORCHESTRATOR runtime with typed scope, allowlisted operations, replayable evidence, and fail-closed external gates."
policy:
  allow_implicit_invocation: true
