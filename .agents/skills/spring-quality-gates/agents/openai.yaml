interface:
  display_name: "Spring Quality Gates"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $spring-quality-gates for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
