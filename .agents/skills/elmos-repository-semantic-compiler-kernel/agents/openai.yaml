interface:
  display_name: "Repository Semantic Compiler Kernel"
  short_description: "Run ELMOS-V3-003 with proof-driven controls"
  default_prompt: "Use $elmos-repository-semantic-compiler-kernel through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
