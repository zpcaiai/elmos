interface:
  display_name: "elmos-io-environment-observable-semantics"
  short_description: "Normalize observable console, file, environment, process and network interactions for source-targ..."
  default_prompt: "Use $elmos-io-environment-observable-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-io-environment-observable-semantics"
  handler_id: "execute_elmos_io_environment_observable_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
