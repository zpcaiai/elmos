interface:
  display_name: "Durable Tool Settlement"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $durable-tool-settlement for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
