interface:
  display_name: "Fine Grained Authorization Engine"
  short_description: "Run fine-grained-authorization-engine with bounded commercial controls"
  default_prompt: "Use $fine-grained-authorization-engine through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
