interface:
  display_name: "ELMOS 调试记录、检查点与运行回放"
  short_description: "Guide 调试记录、检查点与运行回放 with evidence controls"
  default_prompt: "Use $elmos-debug-record-replay as guidance for 调试记录、检查点与运行回放 with the pinned Project Intelligence contracts. Treat dependencies as implementation prerequisites, not permission or automatic execution; keep declared, executed, external, and certified states separate."
