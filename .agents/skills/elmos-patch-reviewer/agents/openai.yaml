interface:
  display_name: Patch Reviewer
  short_description: Run bounded Patch Reviewer repository capability
  default_prompt: Use $elmos-patch-reviewer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
