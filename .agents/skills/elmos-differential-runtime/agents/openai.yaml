interface:
  display_name: "elmos-differential-runtime"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-differential-runtime within its imported blueprint boundary; require real runtime and external evidence before completion claims."
