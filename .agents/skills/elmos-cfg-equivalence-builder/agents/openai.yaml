interface:
  display_name: "elmos-cfg-equivalence-builder"
  short_description: "Build normalized source/target CFGs and compare branch, loop, early-exit and exceptional control-..."
  default_prompt: "Use $elmos-cfg-equivalence-builder through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-cfg-equivalence-builder"
  handler_id: "execute_elmos_cfg_equivalence_builder"
  operation: "GRAPH_ANALYSIS"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
