interface:
  display_name: Task Dag Builder
  short_description: Run bounded Task Dag Builder repository capability
  default_prompt: Use $elmos-task-dag-builder with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
