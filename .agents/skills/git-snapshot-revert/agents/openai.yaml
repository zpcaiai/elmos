interface:
  display_name: "Git Snapshot Revert"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $git-snapshot-revert for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
