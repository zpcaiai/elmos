interface:
  display_name: "Constrained Capture Matching"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $constrained-capture-matching for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
