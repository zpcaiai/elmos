interface:
  display_name: "Dependency Credential Propagation"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $dependency-credential-propagation for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
