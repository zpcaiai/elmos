interface:
  display_name: "elmos-closure-capture-lambda-semantics"
  short_description: "Preserve capture-by-value/reference, receiver capture, mutable closures and escaping lifetime beh..."
  default_prompt: "Use $elmos-closure-capture-lambda-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-closure-capture-lambda-semantics"
  handler_id: "execute_elmos_closure_capture_lambda_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
