interface:
  display_name: "Mutation Strength Certification"
  short_description: "Run mutation-strength-certification with bounded commercial controls"
  default_prompt: "Use $mutation-strength-certification through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
