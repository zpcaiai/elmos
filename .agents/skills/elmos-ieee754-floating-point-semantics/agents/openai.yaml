interface:
  display_name: "elmos-ieee754-floating-point-semantics"
  short_description: "Preserve NaN, infinities, signed zero, rounding, fused operations, extended precision and toleran..."
  default_prompt: "Use $elmos-ieee754-floating-point-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-ieee754-floating-point-semantics"
  handler_id: "execute_elmos_ieee754_floating_point_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
