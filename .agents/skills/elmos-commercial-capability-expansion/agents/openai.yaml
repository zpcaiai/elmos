interface:
  display_name: "Elmos Commercial Capability Expansion"
  short_description: "Run elmos-commercial-capability-expansion with bounded commercial controls"
  default_prompt: "Use $elmos-commercial-capability-expansion as non-executable orchestration guidance: traverse the normalized dependency DAG, prepare each exact invocation, and submit only authority-bound exact calls through CommercialCapabilityExpansionService.execute."
policy:
  allow_implicit_invocation: true
