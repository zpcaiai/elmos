interface:
  display_name: "Minimal Lockfile Delta"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $minimal-lockfile-delta for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
