interface:
  display_name: "Repository Knowledge Precompute"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $repository-knowledge-precompute for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
