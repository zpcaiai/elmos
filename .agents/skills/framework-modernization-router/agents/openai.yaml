interface:
  display_name: "Framework Modernization Router"
  short_description: "Run framework-modernization-router with bounded commercial controls"
  default_prompt: "Use $framework-modernization-router through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
