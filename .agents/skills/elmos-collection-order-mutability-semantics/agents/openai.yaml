interface:
  display_name: "elmos-collection-order-mutability-semantics"
  short_description: "Preserve sequence/set/map ordering, equality, hashing, mutability, aliasing and iteration guarant..."
  default_prompt: "Use $elmos-collection-order-mutability-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-collection-order-mutability-semantics"
  handler_id: "execute_elmos_collection_order_mutability_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
