interface:
  display_name: Model Selection Controller
  short_description: Run bounded Model Selection Controller repository capability
  default_prompt: Use $elmos-model-selection-controller with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
