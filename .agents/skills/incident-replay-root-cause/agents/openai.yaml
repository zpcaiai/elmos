interface:
  display_name: "Incident Replay Root Cause"
  short_description: "Run incident-replay-root-cause with bounded commercial controls"
  default_prompt: "Use $incident-replay-root-cause through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
