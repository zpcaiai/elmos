interface:
  display_name: "Spring Legacy Modernization Domain Pack"
  short_description: "Run ELMOS-V3-009 with proof-driven controls"
  default_prompt: "Use $elmos-domain-spring-legacy-modernization through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
