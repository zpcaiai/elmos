interface:
  display_name: "Recipe Preconditions"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $recipe-preconditions for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
