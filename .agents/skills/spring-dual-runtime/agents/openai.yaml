interface:
  display_name: "Spring Dual Runtime"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $spring-dual-runtime for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
