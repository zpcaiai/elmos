interface:
  display_name: "Multi Engine Rewrite Router"
  short_description: "Run multi-engine-rewrite-router with bounded commercial controls"
  default_prompt: "Use $multi-engine-rewrite-router through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
