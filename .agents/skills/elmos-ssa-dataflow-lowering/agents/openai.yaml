interface:
  display_name: "elmos-ssa-dataflow-lowering"
  short_description: "Lower relevant code into SSA-like dataflow form to expose definitions, uses, phi merges and value..."
  default_prompt: "Use $elmos-ssa-dataflow-lowering through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-ssa-dataflow-lowering"
  handler_id: "execute_elmos_ssa_dataflow_lowering"
  operation: "GRAPH_ANALYSIS"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
