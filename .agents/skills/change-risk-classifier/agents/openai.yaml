interface:
  display_name: "Change Risk Classifier"
  short_description: "Run change-risk-classifier with bounded commercial controls"
  default_prompt: "Use $change-risk-classifier through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
