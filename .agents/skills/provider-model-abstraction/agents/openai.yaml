interface:
  display_name: "Provider Model Abstraction"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $provider-model-abstraction for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
