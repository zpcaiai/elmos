interface:
  display_name: "Repository Slicing Context Pack"
  short_description: "Run repository-slicing-context-pack with bounded commercial controls"
  default_prompt: "Use $repository-slicing-context-pack through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
