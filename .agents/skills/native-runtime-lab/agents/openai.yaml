interface:
  display_name: "Native Runtime Lab"
  short_description: "Run native-runtime-lab with bounded commercial controls"
  default_prompt: "Use $native-runtime-lab through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
