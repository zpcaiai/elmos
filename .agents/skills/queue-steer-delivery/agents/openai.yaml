interface:
  display_name: "Queue Steer Delivery"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $queue-steer-delivery for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
