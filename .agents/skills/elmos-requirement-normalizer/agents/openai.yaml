interface:
  display_name: Requirement Normalizer
  short_description: Run bounded Requirement Normalizer repository capability
  default_prompt: Use $elmos-requirement-normalizer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
