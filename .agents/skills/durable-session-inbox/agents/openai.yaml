interface:
  display_name: "Durable Session Inbox"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $durable-session-inbox for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
