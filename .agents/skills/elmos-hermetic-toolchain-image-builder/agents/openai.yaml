interface:
  display_name: "elmos-hermetic-toolchain-image-builder"
  short_description: "Build content-addressed toolchain/runtime images or VM descriptors with pinned compilers, SDKs, l..."
  default_prompt: "Use $elmos-hermetic-toolchain-image-builder through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-hermetic-toolchain-image-builder"
  handler_id: "execute_elmos_hermetic_toolchain_image_builder"
  operation: "NATIVE_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
