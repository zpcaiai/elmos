interface:
  display_name: "Batch 41: Pattern Antipattern Extraction"
  short_description: "Run evidence-bound Batch 41 pattern antipattern extraction work"
  default_prompt: "Use $b41-pattern-antipattern-extraction to implement and verify this capability with fail-closed evidence."
