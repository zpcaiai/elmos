interface:
  display_name: "Elmos ETA Estimator"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-eta-estimator to perform its bounded repository workflow with provenance and fail-closed evidence."
