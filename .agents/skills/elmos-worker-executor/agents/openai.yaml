interface:
  display_name: "Elmos Worker Executor"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-worker-executor to perform its bounded repository workflow with provenance and fail-closed evidence."
