interface:
  display_name: "Scanning Recipe Accumulator"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $scanning-recipe-accumulator for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
