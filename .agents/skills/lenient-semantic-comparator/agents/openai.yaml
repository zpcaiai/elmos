interface:
  display_name: "Lenient Semantic Comparator"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $lenient-semantic-comparator for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
