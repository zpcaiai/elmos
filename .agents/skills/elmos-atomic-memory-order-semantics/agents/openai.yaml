interface:
  display_name: "elmos-atomic-memory-order-semantics"
  short_description: "Map relaxed/acquire/release/seq-cst atomics and language-specific volatile primitives without str..."
  default_prompt: "Use $elmos-atomic-memory-order-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-atomic-memory-order-semantics"
  handler_id: "execute_elmos_atomic_memory_order_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
