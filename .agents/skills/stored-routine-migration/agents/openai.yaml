interface:
  display_name: "Stored Routine Migration"
  short_description: "Run stored-routine-migration with bounded commercial controls"
  default_prompt: "Use $stored-routine-migration through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
