interface:
  display_name: "elmos-deterministic-replay-oracle"
  short_description: "Record and replay external nondeterministic inputs and schedules where possible to turn flaky dif..."
  default_prompt: "Use $elmos-deterministic-replay-oracle through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-deterministic-replay-oracle"
  handler_id: "execute_elmos_deterministic_replay_oracle"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
