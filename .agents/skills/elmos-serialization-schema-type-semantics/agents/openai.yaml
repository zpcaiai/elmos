interface:
  display_name: "elmos-serialization-schema-type-semantics"
  short_description: "Tie in-memory types to JSON/XML/Protobuf/Avro/DB/wire schemas including defaults, unknown fields..."
  default_prompt: "Use $elmos-serialization-schema-type-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-serialization-schema-type-semantics"
  handler_id: "execute_elmos_serialization_schema_type_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
