interface:
  display_name: "elmos-specialized-agent-registry"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-specialized-agent-registry within its imported blueprint boundary; require real runtime and external evidence before completion claims."
