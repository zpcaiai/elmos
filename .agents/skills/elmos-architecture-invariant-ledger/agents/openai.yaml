interface:
  display_name: "Elmos Architecture Invariant Ledger"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-architecture-invariant-ledger to perform its bounded repository workflow with provenance and fail-closed evidence."
