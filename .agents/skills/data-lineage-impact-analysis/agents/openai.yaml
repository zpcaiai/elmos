interface:
  display_name: "Data Lineage Impact Analysis"
  short_description: "Run data-lineage-impact-analysis with bounded commercial controls"
  default_prompt: "Use $data-lineage-impact-analysis through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
