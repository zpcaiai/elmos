interface:
  display_name: "elmos-nullability-optionality-semantics"
  short_description: "Model null, undefined, optional, nullable reference/value types and null-propagation behavior exp..."
  default_prompt: "Use $elmos-nullability-optionality-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-nullability-optionality-semantics"
  handler_id: "execute_elmos_nullability_optionality_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
