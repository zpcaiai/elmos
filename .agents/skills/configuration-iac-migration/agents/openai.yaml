interface:
  display_name: "Configuration Iac Migration"
  short_description: "Run configuration-iac-migration with bounded commercial controls"
  default_prompt: "Use $configuration-iac-migration through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
