interface:
  display_name: "Hermetic Build Environment"
  short_description: "Run hermetic-build-environment with bounded commercial controls"
  default_prompt: "Use $hermetic-build-environment through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
