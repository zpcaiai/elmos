interface:
  display_name: "elmos-observable-behavior-specification"
  short_description: "Define route-specific observables and comparison relations so equivalence means the same external..."
  default_prompt: "Use $elmos-observable-behavior-specification through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-observable-behavior-specification"
  handler_id: "execute_elmos_observable_behavior_specification"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
