interface:
  display_name: "Skill Promotion Canary"
  short_description: "Run skill-promotion-canary with bounded commercial controls"
  default_prompt: "Use $skill-promotion-canary through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
