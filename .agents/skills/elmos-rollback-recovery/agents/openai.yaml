interface:
  display_name: Rollback Recovery
  short_description: Run bounded Rollback Recovery repository capability
  default_prompt: Use $elmos-rollback-recovery with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
