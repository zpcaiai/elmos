interface:
  display_name: "Build Model Intelligence"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $build-model-intelligence for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
