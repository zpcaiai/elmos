interface:
  display_name: "Harness Intelligence"
  short_description: "Route tools, models, prompts, context, and benchmarks under explicit policy."
  default_prompt: "Use $elmos-harness-intelligence through the exact PDHI v1 K8 runtime with typed scope, allowlisted operations, replayable evidence, and fail-closed external gates."
policy:
  allow_implicit_invocation: true
