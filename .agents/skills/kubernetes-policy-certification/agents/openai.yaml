interface:
  display_name: "Kubernetes Policy Certification"
  short_description: "Run kubernetes-policy-certification with bounded commercial controls"
  default_prompt: "Use $kubernetes-policy-certification through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
