interface:
  display_name: Repository Certifier
  short_description: Run bounded Repository Certifier repository capability
  default_prompt: Use $elmos-repository-certifier with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
