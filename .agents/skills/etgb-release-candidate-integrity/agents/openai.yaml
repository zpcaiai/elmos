interface:
  display_name: "ETGB Release Candidate Integrity"
  short_description: "Run ETGB full product assurance with evidence controls"
  default_prompt: "Use $etgb-release-candidate-integrity to run this ETGB capability with fail-closed evidence."
policy:
  allow_implicit_invocation: true
