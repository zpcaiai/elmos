skill: elmos-assurance-requirement-oracles
tasks:
- id: elmos-assurance-requirement-oracles-T01
  action: 将需求拆成可证伪claims、状态/前后置条件、错误与effect；保留原文span。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-requirement-oracles-T02
  action: 标记normative/compatibility/invariant/metamorphic/reference/LLM-proposal来源。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-requirement-oracles-T03
  action: 独立批准expected和关系前提，不允许从target返回值反向填expected。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-requirement-oracles-T04
  action: 规范、源行为或安全要求冲突时创建finding；批准行为变更单独建回归。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
global_contracts:
- contracts/schemas/run-request.schema.json
- contracts/schemas/evidence-payload.schema.json
constraints:
- reuse-existing-architecture
- fail-closed
- no-hidden-test-leak
- no-unapproved-side-effects
- no-fabricated-runtime-evidence
