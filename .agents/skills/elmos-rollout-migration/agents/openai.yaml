interface:
  display_name: "Billing Rollout and Migration"
  short_description: "Apply imported Elmos billing guidance safely"
  default_prompt: "Use $elmos-rollout-migration for the requested billing scope; treat Bxx IDs as elmos.pricing-billing.v1; use the repository runtime binding for local evidence and keep external evidence NOT_RUN until independently verified."
policy:
  allow_implicit_invocation: true
