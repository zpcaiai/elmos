interface:
  display_name: "Transformation Before After Tests"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $transformation-before-after-tests for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
