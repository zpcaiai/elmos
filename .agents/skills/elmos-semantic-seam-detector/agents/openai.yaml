interface:
  display_name: "Elmos Semantic Seam Detector"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-semantic-seam-detector to perform its bounded repository workflow with provenance and fail-closed evidence."
