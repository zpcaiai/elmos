interface:
  display_name: "Elmos Model Registry Guard"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-model-registry-guard to perform its bounded repository workflow with provenance and fail-closed evidence."
