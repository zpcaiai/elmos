interface:
  display_name: Model Registry Guard
  short_description: Run bounded Model Registry Guard repository capability
  default_prompt: Use $elmos-model-registry-guard with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
