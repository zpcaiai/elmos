interface:
  display_name: "elmos-iterator-generator-coroutine-semantics"
  short_description: "Model suspension points, yielded values, cleanup, backpressure and resumption state across genera..."
  default_prompt: "Use $elmos-iterator-generator-coroutine-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-iterator-generator-coroutine-semantics"
  handler_id: "execute_elmos_iterator_generator_coroutine_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
