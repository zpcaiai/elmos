interface:
  display_name: "Variadic Pattern Matching"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $variadic-pattern-matching for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
