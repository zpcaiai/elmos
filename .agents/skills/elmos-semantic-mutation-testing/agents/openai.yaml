interface:
  display_name: "elmos-semantic-mutation-testing"
  short_description: "Inject behavior-changing mutations into conversion rules/targets and verify that equivalence test..."
  default_prompt: "Use $elmos-semantic-mutation-testing through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-semantic-mutation-testing"
  handler_id: "execute_elmos_semantic_mutation_testing"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
