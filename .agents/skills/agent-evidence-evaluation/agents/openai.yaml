interface:
  display_name: "Agent Evidence Evaluation"
  short_description: "Run agent-evidence-evaluation with bounded commercial controls"
  default_prompt: "Use $agent-evidence-evaluation through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
