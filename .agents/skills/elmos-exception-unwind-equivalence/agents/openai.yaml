interface:
  display_name: "elmos-exception-unwind-equivalence"
  short_description: "Preserve exception matching, stack unwinding, finally/defer/destructor behavior, retry boundaries..."
  default_prompt: "Use $elmos-exception-unwind-equivalence through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-exception-unwind-equivalence"
  handler_id: "execute_elmos_exception_unwind_equivalence"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
