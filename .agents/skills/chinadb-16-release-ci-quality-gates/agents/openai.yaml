interface:
  display_name: "ChinaDB 16 Release CI Quality Gates"
  short_description: "Run this ChinaDB migration Skill with evidence controls"
  default_prompt: "Use $chinadb-16-release-ci-quality-gates as a SPEC_ONLY ChinaDB migration contract; preserve exact route semantics and fail closed while real evidence is NOT_RUN."
