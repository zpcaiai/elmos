interface:
  display_name: "Session Run Coordinator"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $session-run-coordinator for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
