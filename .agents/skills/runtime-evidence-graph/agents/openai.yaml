interface:
  display_name: "Runtime Evidence Graph"
  short_description: "Run runtime-evidence-graph with bounded commercial controls"
  default_prompt: "Use $runtime-evidence-graph through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
