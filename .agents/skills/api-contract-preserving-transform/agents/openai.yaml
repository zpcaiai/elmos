interface:
  display_name: "Api Contract Preserving Transform"
  short_description: "Run api-contract-preserving-transform with bounded commercial controls"
  default_prompt: "Use $api-contract-preserving-transform through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
