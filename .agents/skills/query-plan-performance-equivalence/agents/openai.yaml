interface:
  display_name: "Query Plan Performance Equivalence"
  short_description: "Run query-plan-performance-equivalence with bounded commercial controls"
  default_prompt: "Use $query-plan-performance-equivalence through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
