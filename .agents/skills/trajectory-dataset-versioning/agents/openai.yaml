interface:
  display_name: "Trajectory Dataset Versioning"
  short_description: "Run trajectory-dataset-versioning with bounded commercial controls"
  default_prompt: "Use $trajectory-dataset-versioning through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
