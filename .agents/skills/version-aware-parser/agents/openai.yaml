interface:
  display_name: "Version Aware Parser"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $version-aware-parser for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
