interface:
  display_name: "elmos-message-event-equivalence"
  short_description: "Compare queues, topics, event envelopes, ordering, delivery semantics and deduplication outcomes."
  default_prompt: "Use $elmos-message-event-equivalence through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-message-event-equivalence"
  handler_id: "execute_elmos_message_event_equivalence"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
