interface:
  display_name: "elmos-headless-runtime-api"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-headless-runtime-api within its imported blueprint boundary; require real runtime and external evidence before completion claims."
