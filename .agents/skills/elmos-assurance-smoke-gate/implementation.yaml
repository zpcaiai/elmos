skill: elmos-assurance-smoke-gate
tasks:
- id: elmos-assurance-smoke-gate-T01
  action: 按风险/依赖中心性和估算机器成本选最小覆盖关键义务集合，不随机挑case。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-smoke-gate-T02
  action: 包含启动、真实依赖、鉴权与至少一条关键业务effect canary。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-smoke-gate-T03
  action: 在冻结artifact上运行并验证assertions、cleanup、manifest完整性。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-smoke-gate-T04
  action: 缺失/失败时阻断依赖运行健康的昂贵任务；保留证据，允许独立静态/规则证明继续。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
global_contracts:
- contracts/schemas/run-request.schema.json
- contracts/schemas/evidence-payload.schema.json
constraints:
- reuse-existing-architecture
- fail-closed
- no-hidden-test-leak
- no-unapproved-side-effects
- no-fabricated-runtime-evidence
