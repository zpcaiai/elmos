interface:
  display_name: Security Auth Gate
  short_description: Run bounded Security Auth Gate repository capability
  default_prompt: Use $elmos-security-auth-gate with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
