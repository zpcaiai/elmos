interface:
  display_name: "Spring Target Profile"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $spring-target-profile for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
