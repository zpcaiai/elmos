interface:
  display_name: "Progressive Skill Disclosure"
  short_description: "Run progressive-skill-disclosure with bounded commercial controls"
  default_prompt: "Use $progressive-skill-disclosure through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
