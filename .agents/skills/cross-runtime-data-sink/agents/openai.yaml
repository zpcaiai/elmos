interface:
  display_name: "Cross Runtime Data Sink"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $cross-runtime-data-sink for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
