interface:
  display_name: "Behavioral Equivalence Migration"
  short_description: "Run behavioral-equivalence-migration with bounded commercial controls"
  default_prompt: "Use $behavioral-equivalence-migration through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
