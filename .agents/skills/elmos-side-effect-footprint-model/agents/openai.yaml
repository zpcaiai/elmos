interface:
  display_name: "elmos-side-effect-footprint-model"
  short_description: "Classify reads/writes to memory, DB, files, network, queues, clocks, environment and external sys..."
  default_prompt: "Use $elmos-side-effect-footprint-model through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-side-effect-footprint-model"
  handler_id: "execute_elmos_side_effect_footprint_model"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
