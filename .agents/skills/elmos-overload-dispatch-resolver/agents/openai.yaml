interface:
  display_name: "elmos-overload-dispatch-resolver"
  short_description: "Model overload resolution, multimethod dispatch, virtual dispatch and target-language call select..."
  default_prompt: "Use $elmos-overload-dispatch-resolver through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-overload-dispatch-resolver"
  handler_id: "execute_elmos_overload_dispatch_resolver"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
