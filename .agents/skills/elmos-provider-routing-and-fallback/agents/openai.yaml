interface:
  display_name: "Elmos Provider Routing and Fallback"
  short_description: "Run this ELMOS Skill with evidence controls"
  default_prompt: "Use $elmos-provider-routing-and-fallback to execute this ELMOS Skill with fail-closed evidence."
policy:
  allow_implicit_invocation: true
