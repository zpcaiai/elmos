interface:
  display_name: "MiniApp State Event Lifecycle Converter"
  short_description: "Run this frontend-to-MiniApp Skill with evidence controls"
  default_prompt: "Use $miniapp-state-event-lifecycle-converter with its compiled contract, dispatch Skill key miniapp-state-event-lifecycle-converter through `engines/frontend-client-engine` via `npm run miniapp` / `executeMiniappSkill`. Treat bounded runtime evidence as DECLARED, while external evidence stays NOT_RUN and certification stays NOT_CERTIFIED until exact Batch 32 gates pass."
