skill: elmos-assurance-coverage-planner
tasks:
- id: elmos-assurance-coverage-planner-T01
  action: 生成Requirement/Interface/Input/State/Permission/Error/Effect/Concurrency/Semantics/Platform覆盖维度。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-coverage-planner-T02
  action: 关键路径显式组合；其余允许经批准t-wise，记录强度而非虚假穷尽。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-coverage-planner-T03
  action: 分开planned/executable/executed/asserted/passed，冻结分母与suite版本。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-coverage-planner-T04
  action: 测算机器预算、不可测义务和缺失oracle，不能为节省成本删除required项。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
global_contracts:
- contracts/schemas/run-request.schema.json
- contracts/schemas/evidence-payload.schema.json
constraints:
- reuse-existing-architecture
- fail-closed
- no-hidden-test-leak
- no-unapproved-side-effects
- no-fabricated-runtime-evidence
