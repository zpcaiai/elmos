interface:
  display_name: Conflict Resolver
  short_description: Run bounded Conflict Resolver repository capability
  default_prompt: Use $elmos-conflict-resolver with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
