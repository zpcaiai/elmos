interface:
  display_name: "Database Security Policy Migration"
  short_description: "Run database-security-policy-migration with bounded commercial controls"
  default_prompt: "Use $database-security-policy-migration through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
