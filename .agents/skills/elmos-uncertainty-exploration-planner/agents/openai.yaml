interface:
  display_name: "Elmos Uncertainty Exploration Planner"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-uncertainty-exploration-planner to perform its bounded repository workflow with provenance and fail-closed evidence."
