interface:
  display_name: "Cost Latency Quality Optimizer"
  short_description: "Run cost-latency-quality-optimizer with bounded commercial controls"
  default_prompt: "Use $cost-latency-quality-optimizer through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
