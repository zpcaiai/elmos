interface:
  display_name: "Semantic Token Context Query"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $semantic-token-context-query for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
