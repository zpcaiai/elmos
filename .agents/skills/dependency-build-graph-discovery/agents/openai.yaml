interface:
  display_name: "Dependency Build Graph Discovery"
  short_description: "Run dependency-build-graph-discovery with bounded commercial controls"
  default_prompt: "Use $dependency-build-graph-discovery through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
