interface:
  display_name: "Context Epoch"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $context-epoch for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
