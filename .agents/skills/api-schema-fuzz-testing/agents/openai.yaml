interface:
  display_name: "Api Schema Fuzz Testing"
  short_description: "Run api-schema-fuzz-testing with bounded commercial controls"
  default_prompt: "Use $api-schema-fuzz-testing through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
