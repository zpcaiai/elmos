---
name: namespace-package-and-naming-planner
description: Plan stable target package, namespace, module, declaration, file, and test names with collision handling. Use before emitting files.
---
# Namespace Package and Naming Planner
Read `../references/skeleton-v1.md`. Apply target/organization conventions while preserving external serialization/API names. Detect reserved words, case folding, simple-name, merge, overload-loss, file/platform and generated-name collisions; resolve explicitly or block. Distinguish Python distribution/import and TypeScript package/module paths. Every rename remains source-mapped and public API changes create obligations.
