interface:
  display_name: "Usage Metering and Cost Events"
  short_description: "Apply imported Elmos billing guidance safely"
  default_prompt: "Use $elmos-usage-metering for the requested billing scope; treat Bxx IDs as elmos.pricing-billing.v1; use the repository runtime binding for local evidence and keep external evidence NOT_RUN until independently verified."
policy:
  allow_implicit_invocation: true
