interface:
  display_name: "Rich Type Attribution"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $rich-type-attribution for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
