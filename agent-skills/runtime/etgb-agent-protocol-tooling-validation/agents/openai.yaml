interface:
  display_name: "ETGB Agent Protocol Tooling Validation"
  short_description: "Run ETGB full product assurance with evidence controls"
  default_prompt: "Use $etgb-agent-protocol-tooling-validation to run this ETGB capability with fail-closed evidence."
policy:
  allow_implicit_invocation: true
