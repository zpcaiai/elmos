interface:
  display_name: "Session and Token Security Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2906-session-and-token-security-validator to run FRT-2906 through the governed FRT runtime with exact scope and fail-closed evidence."
