apiVersion: elmos.ai/v1
kind: ProofCarryingSkill
metadata:
  name: service-virtualization-contract-test
  version: 3.0.0
  pack: 24-api-event-integration-modernization
  priority: P0
  owner: integration-modernization-team
spec:
  description: 为不可用或昂贵依赖生成虚拟服务、故障场景和契约验证。
  kernel: K3 Contract Compiler + K6 Distributed Equivalence
  riskClass: high
  exposure: atomic-registry-only
  businessLines:
  - api-event-integration
  capabilityTags:
  - 24-api-event-integration-modernization
  - api-event-integration
  - contract
  - service
  - test
  - virtualization
  triggers:
  - 任务明确要求：为不可用或昂贵依赖生成虚拟服务、故障场景和契约验证。
  - 任务属于 api-event-integration，需要仓库级、可重放、带证据和回滚的执行。
  - 注册表确认 service-virtualization-contract-test 与目标技术栈、版本、租户和风险相容。
  negativeTriggers:
  - 仅询问概念或写作，不访问仓库、数据、环境或工具。
  - 没有租户授权、版本锁定、基线、验收标准或验证环境。
  - 目标是绕过测试、权限、签名、证据、审计或回滚门。
  - 已有更窄且风险更低的确定性 Skill 能完整处理任务。
  preconditions:
  - tenant.authorized == true
  - task.contract != null
  - release.versionPinned == true
  - baseline.snapshotAvailable == true
  - evidence.serviceAvailable == true
  inputs:
  - name: task contract
    required: true
  - name: tenant and repository identity
    required: true
  - name: version-pinned environment
    required: true
  - name: acceptance and evidence obligations
    required: true
  - name: API, event and consumer contract inventory
    required: true
  outputs:
  - name: execution plan
    contentAddressed: true
  - name: versioned artifacts or patch set
    contentAddressed: true
  - name: verification and evidence bundle
    contentAddressed: true
  - name: risk, cost and machine wall-clock report
    contentAddressed: true
  - name: rollback or compensation record
    contentAddressed: true
  workflow:
  - inventory-contracts-and-consumers
  - characterize-delivery-semantics
  - design-target-contracts
  - generate-adapters-and-bridges
  - contract-and-load-test
  - dual-publish-and-observe
  - certify-or-fallback
  tools:
    allowed:
    - contract.parse
    - integration.trace
    - semantic.transform
    - service.virtualize
    - load.replay
    - security.test
    - evidence.write
    defaultDeny: true
    parameterValidationRequired: true
    environmentOwnedAuthority: true
  dependencies:
  - typed-skill-contract
  - evidence-contract
  - policy-contract
  - skill-transaction-and-rollback
  - tenant-policy-aware-retrieval
  - api-and-event-contract-graph
  - api-contract-ingestion
  - semantic-behavior-equivalence
  invariants:
  - contract-compatible
  - delivery-semantics-preserved
  - security-policy-preserved
  - consumer-tests-pass
  - cutover-reversible
  - tenant-boundary-preserved
  - source-and-target-traceable
  - no-hidden-test-weakening
  - no-evidence-fabrication
  - machine-wall-clock-recorded
  failureModes:
  - unsupported-source-or-target-version
  - insufficient-semantic-or-contract-coverage
  - deterministic-verification-failure
  - security-privacy-or-license-policy-failure
  - performance-or-capacity-regression
  - rollback-or-recovery-evidence-missing
  execution:
    class: durable-replayable
    idempotencyRequired: true
    checkpointRequired: true
    independentVerificationRequired: true
    productionWriteApprovalRequired: true
    maxUnverifiedSideEffects: 0
  evidence:
    requiredGates:
    - contract-compatible
    - delivery-semantics-preserved
    - security-policy-preserved
    - consumer-tests-pass
    - cutover-reversible
    minimumLevel: E3
    sourceBindingRequired: true
    independentReplayRequired: true
  rollback:
    required: true
    strategy: restore-versioned-checkpoint-and-compensate-side-effects
    rehearsalRequired: false
  learning:
    captureTrajectory: true
    globalTrainingEligible: false
    tenantAdapterEligible: explicit-opt-in
    minimumDatasetTier: Gold
    humanAcceptanceRequired: false
  telemetry:
    emitTrace: true
    emitCost: true
    emitWallClock: true
    emitProgress: true
    emitEvidenceLineage: true
    sensitiveContentDefault: redacted
  compatibility:
    packageVersion: 3.0.0
    runtime: Elmos Proof-Driven Agentic Harness v3+
    versionPinned: true
    matrixRequired: true
  maturity:
    status: specification-ready
    runtimeImplementation: required
    certificationTarget: E3
  support:
    supportTier: LTS-candidate
    deprecationPolicyRequired: true
