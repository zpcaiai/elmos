interface:
  display_name: "Elmos Failure Classifier"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-failure-classifier to perform its bounded repository workflow with provenance and fail-closed evidence."
