interface:
  display_name: "Stream Usage Cost Cache Runtime"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $stream-usage-cost-cache-runtime for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
