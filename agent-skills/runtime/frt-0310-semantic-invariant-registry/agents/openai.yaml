interface:
  display_name: "Semantic Invariant Registry"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0310-semantic-invariant-registry to run FRT-0310 through the governed FRT runtime with exact scope and fail-closed evidence."
