interface:
  display_name: "Parser Tck"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $parser-tck for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
