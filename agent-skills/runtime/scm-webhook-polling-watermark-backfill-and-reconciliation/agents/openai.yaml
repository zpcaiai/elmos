interface:
  display_name: "ELMOS Product B35: Scm Webhook Polling Watermark Backfill And Reconciliation"
  short_description: "Run ELMOS Product B35 contract with fail-closed evidence"
  default_prompt: "Use $scm-webhook-polling-watermark-backfill-and-reconciliation to implement its fail-closed ELMOS Product B35 contract."
