interface:
  display_name: "elmos-reconciliation-scheduler"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-reconciliation-scheduler within its imported blueprint boundary; require real runtime and external evidence before completion claims."
