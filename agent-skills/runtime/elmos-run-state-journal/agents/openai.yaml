interface:
  display_name: Run State Journal
  short_description: Run bounded Run State Journal repository capability
  default_prompt: Use $elmos-run-state-journal with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
