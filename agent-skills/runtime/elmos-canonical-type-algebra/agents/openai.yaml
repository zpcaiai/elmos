interface:
  display_name: "elmos-canonical-type-algebra"
  short_description: "Normalize primitive, nominal, structural, algebraic, callable, reference and opaque types into a..."
  default_prompt: "Use $elmos-canonical-type-algebra through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-canonical-type-algebra"
  handler_id: "execute_elmos_canonical_type_algebra"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
