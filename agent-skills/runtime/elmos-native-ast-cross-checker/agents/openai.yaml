interface:
  display_name: "elmos-native-ast-cross-checker"
  short_description: "Cross-check ELMOS CST/AST extraction against native compiler or language-service frontends to det..."
  default_prompt: "Use $elmos-native-ast-cross-checker through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-native-ast-cross-checker"
  handler_id: "execute_elmos_native_ast_cross_checker"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
