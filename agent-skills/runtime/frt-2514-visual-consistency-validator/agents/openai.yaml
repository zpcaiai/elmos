interface:
  display_name: "Visual Consistency Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2514-visual-consistency-validator to run FRT-2514 through the governed FRT runtime with exact scope and fail-closed evidence."
