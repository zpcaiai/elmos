interface:
  display_name: "SAST Coordinator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2913-sast-coordinator to run FRT-2913 through the governed FRT runtime with exact scope and fail-closed evidence."
