interface:
  display_name: "Vue 3 Architecture Synthesizer"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0602-vue-3-architecture-synthesizer to run FRT-0602 through the governed FRT runtime with exact scope and fail-closed evidence."
