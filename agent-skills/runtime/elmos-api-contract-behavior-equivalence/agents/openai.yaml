interface:
  display_name: "elmos-api-contract-behavior-equivalence"
  short_description: "Verify status/error mapping, validation, pagination, authentication, compatibility and schema beh..."
  default_prompt: "Use $elmos-api-contract-behavior-equivalence through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-api-contract-behavior-equivalence"
  handler_id: "execute_elmos_api_contract_behavior_equivalence"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
