interface:
  display_name: "elmos-specialized-model-training"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-specialized-model-training within its imported blueprint boundary; require real runtime and external evidence before completion claims."
