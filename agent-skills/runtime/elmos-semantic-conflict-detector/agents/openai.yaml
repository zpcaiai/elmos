interface:
  display_name: Semantic Conflict Detector
  short_description: Run bounded Semantic Conflict Detector repository capability
  default_prompt: Use $elmos-semantic-conflict-detector with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
