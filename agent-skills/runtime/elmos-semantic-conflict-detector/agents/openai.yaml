interface:
  display_name: "Elmos Semantic Conflict Detector"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-semantic-conflict-detector to perform its bounded repository workflow with provenance and fail-closed evidence."
