interface:
  display_name: "elmos-os-arch-libc-matrix"
  short_description: "Test platform-sensitive routes across OS, CPU architecture, endianness and libc/runtime implement..."
  default_prompt: "Use $elmos-os-arch-libc-matrix through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-os-arch-libc-matrix"
  handler_id: "execute_elmos_os_arch_libc_matrix"
  operation: "NATIVE_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
