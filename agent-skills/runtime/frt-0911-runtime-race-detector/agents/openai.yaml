interface:
  display_name: "Runtime Race Detector"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0911-runtime-race-detector to run FRT-0911 through the governed FRT runtime with exact scope and fail-closed evidence."
