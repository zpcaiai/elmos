interface:
  display_name: "Cross-Tenant Data Isolation Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2314-cross-tenant-data-isolation-validator to run FRT-2314 through the governed FRT runtime with exact scope and fail-closed evidence."
