skill: elmos-assurance-generation-domain
tasks:
- id: elmos-assurance-generation-domain-T01
  action: 将spec/contract/state/oracle审批放在代码生成之前。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-generation-domain-T02
  action: 分别生成实现与测试候选，独立规范审核降低共同错误。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-generation-domain-T03
  action: 验收真实DB/消息/权限/UI流程；不同生成语言分别对规范测试，不互相作唯一Oracle。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
- id: elmos-assurance-generation-domain-T04
  action: 落实domain-packs/project-generation全部critical cases，再逐个扩展目标语言。
  implementation_target: 由bootstrap repo-map映射到真实模块；不得假设已有路径
  required_change_types:
  - production-code-or-explicit-reuse
  - tests
  - observability-and-error-path
  done_when: 关联验收场景在真实集成范围通过并保存原始证据
  status: NOT_STARTED
global_contracts:
- contracts/schemas/run-request.schema.json
- contracts/schemas/evidence-payload.schema.json
constraints:
- reuse-existing-architecture
- fail-closed
- no-hidden-test-leak
- no-unapproved-side-effects
- no-fabricated-runtime-evidence
