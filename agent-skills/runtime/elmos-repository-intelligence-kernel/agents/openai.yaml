interface:
  display_name: "Repository Intelligence Kernel"
  short_description: "Run ELMOS-V3-002 with proof-driven controls"
  default_prompt: "Use $elmos-repository-intelligence-kernel through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
