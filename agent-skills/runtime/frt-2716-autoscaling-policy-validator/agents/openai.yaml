interface:
  display_name: "Autoscaling Policy Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2716-autoscaling-policy-validator to run FRT-2716 through the governed FRT runtime with exact scope and fail-closed evidence."
