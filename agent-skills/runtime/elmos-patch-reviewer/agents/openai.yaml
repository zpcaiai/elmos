interface:
  display_name: "Elmos Patch Reviewer"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-patch-reviewer to perform its bounded repository workflow with provenance and fail-closed evidence."
