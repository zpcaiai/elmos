interface:
  display_name: "Component Semantics Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0800-component-semantics-orchestrator to run FRT-0800 through the governed FRT runtime with exact scope and fail-closed evidence."
