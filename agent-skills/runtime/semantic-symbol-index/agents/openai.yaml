interface:
  display_name: "Semantic Symbol Index"
  short_description: "Run semantic-symbol-index with bounded commercial controls"
  default_prompt: "Use $semantic-symbol-index through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
