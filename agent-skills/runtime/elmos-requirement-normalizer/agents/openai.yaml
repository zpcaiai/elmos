interface:
  display_name: "Elmos Requirement Normalizer"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-requirement-normalizer to perform its bounded repository workflow with provenance and fail-closed evidence."
