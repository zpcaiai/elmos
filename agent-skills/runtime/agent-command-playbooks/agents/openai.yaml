interface:
  display_name: "Agent Command Playbooks"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $agent-command-playbooks for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
