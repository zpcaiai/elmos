interface:
  display_name: "Otel Agent Execution Tracing"
  short_description: "Run otel-agent-execution-tracing with bounded commercial controls"
  default_prompt: "Use $otel-agent-execution-tracing through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
