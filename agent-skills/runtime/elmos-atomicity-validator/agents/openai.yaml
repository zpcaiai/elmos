interface:
  display_name: "Elmos Atomicity Validator"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-atomicity-validator to perform its bounded repository workflow with provenance and fail-closed evidence."
