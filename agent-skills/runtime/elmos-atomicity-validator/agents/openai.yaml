interface:
  display_name: Atomicity Validator
  short_description: Run bounded Atomicity Validator repository capability
  default_prompt: Use $elmos-atomicity-validator with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
