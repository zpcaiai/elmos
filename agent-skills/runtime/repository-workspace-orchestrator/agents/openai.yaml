interface:
  display_name: "ELMOS Product B35: Repository Workspace Orchestrator"
  short_description: "Run ELMOS Product B35 contract with fail-closed evidence"
  default_prompt: "Use $repository-workspace-orchestrator to implement its fail-closed ELMOS Product B35 contract."
