interface:
  display_name: "Lifecycle Ordering Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0904-lifecycle-ordering-validator to run FRT-0904 through the governed FRT runtime with exact scope and fail-closed evidence."
