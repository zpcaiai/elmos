interface:
  display_name: "elmos-wasm-portable-semantics-oracle"
  short_description: "Use WebAssembly’s specified validation/execution semantics and reference tests as an optional por..."
  default_prompt: "Use $elmos-wasm-portable-semantics-oracle through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-wasm-portable-semantics-oracle"
  handler_id: "execute_elmos_wasm_portable_semantics_oracle"
  operation: "FORMAL_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
