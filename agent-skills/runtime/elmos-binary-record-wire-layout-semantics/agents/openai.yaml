interface:
  display_name: "elmos-binary-record-wire-layout-semantics"
  short_description: "Preserve fixed records, copybooks, packed fields, alignment, endian, protocol framing and exact b..."
  default_prompt: "Use $elmos-binary-record-wire-layout-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-binary-record-wire-layout-semantics"
  handler_id: "execute_elmos_binary_record_wire_layout_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
