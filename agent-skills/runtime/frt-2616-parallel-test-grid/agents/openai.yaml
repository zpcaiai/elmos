interface:
  display_name: "Parallel Test Grid"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2616-parallel-test-grid to run FRT-2616 through the governed FRT runtime with exact scope and fail-closed evidence."
