interface:
  display_name: "Admin Capability Matrix"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2401-admin-capability-matrix to run FRT-2401 through the governed FRT runtime with exact scope and fail-closed evidence."
