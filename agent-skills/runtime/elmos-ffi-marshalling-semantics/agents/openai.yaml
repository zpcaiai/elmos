interface:
  display_name: "elmos-ffi-marshalling-semantics"
  short_description: "Generate and verify foreign-function bridges, marshalling, ownership transfer, callbacks and erro..."
  default_prompt: "Use $elmos-ffi-marshalling-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-ffi-marshalling-semantics"
  handler_id: "execute_elmos_ffi_marshalling_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
