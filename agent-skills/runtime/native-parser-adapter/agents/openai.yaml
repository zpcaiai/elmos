interface:
  display_name: "Native Parser Adapter"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $native-parser-adapter for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
