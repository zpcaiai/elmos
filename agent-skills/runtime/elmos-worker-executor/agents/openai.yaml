interface:
  display_name: Worker Executor
  short_description: Run bounded Worker Executor repository capability
  default_prompt: Use $elmos-worker-executor with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
