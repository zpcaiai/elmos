interface:
  display_name: "elmos-failure-reducer-minimizer"
  short_description: "Automatically minimize failing source programs, inputs, repository slices and traces while preser..."
  default_prompt: "Use $elmos-failure-reducer-minimizer through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-failure-reducer-minimizer"
  handler_id: "execute_elmos_failure_reducer_minimizer"
  operation: "FUZZ_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
