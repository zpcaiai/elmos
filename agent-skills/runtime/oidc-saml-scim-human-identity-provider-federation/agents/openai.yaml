interface:
  display_name: "ELMOS Product B34: Oidc Saml Scim Human Identity Provider Federation"
  short_description: "Run ELMOS Product B34 contract with fail-closed evidence"
  default_prompt: "Use $oidc-saml-scim-human-identity-provider-federation to implement its fail-closed ELMOS Product B34 contract."
