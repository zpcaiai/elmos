interface:
  display_name: "elmos-type-semantic-loss-gate"
  short_description: "Aggregate type-system obligations and block routes that narrow domains, erase contracts or alter..."
  default_prompt: "Use $elmos-type-semantic-loss-gate through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-type-semantic-loss-gate"
  handler_id: "execute_elmos_type_semantic_loss_gate"
  operation: "GATE_EVALUATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
