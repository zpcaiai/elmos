interface:
  display_name: "Authorization and Policy Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2904-authorization-and-policy-validator to run FRT-2904 through the governed FRT runtime with exact scope and fail-closed evidence."
