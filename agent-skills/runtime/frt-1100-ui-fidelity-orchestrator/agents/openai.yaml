interface:
  display_name: "UI Fidelity Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1100-ui-fidelity-orchestrator to run FRT-1100 through the governed FRT runtime with exact scope and fail-closed evidence."
