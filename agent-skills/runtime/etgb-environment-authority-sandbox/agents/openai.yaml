interface:
  display_name: "ETGB Environment Authority Sandbox"
  short_description: "Run ETGB assurance with evidence controls"
  default_prompt: "Use $etgb-environment-authority-sandbox to run this ETGB capability with fail-closed evidence."
policy:
  allow_implicit_invocation: true
