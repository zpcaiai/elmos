interface:
  display_name: "Platform Template Generator"
  short_description: "Run platform-template-generator with bounded commercial controls"
  default_prompt: "Use $platform-template-generator through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
