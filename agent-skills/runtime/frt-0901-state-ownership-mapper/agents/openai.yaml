interface:
  display_name: "State Ownership Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0901-state-ownership-mapper to run FRT-0901 through the governed FRT runtime with exact scope and fail-closed evidence."
