interface:
  display_name: "License Compliance Scanner"
  short_description: "Run license-compliance-scanner with bounded commercial controls"
  default_prompt: "Use $license-compliance-scanner through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
