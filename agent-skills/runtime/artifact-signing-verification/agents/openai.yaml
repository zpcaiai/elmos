interface:
  display_name: "Artifact Signing Verification"
  short_description: "Run artifact-signing-verification with bounded commercial controls"
  default_prompt: "Use $artifact-signing-verification through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
