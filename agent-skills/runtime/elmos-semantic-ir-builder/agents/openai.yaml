interface:
  display_name: "elmos-semantic-ir-builder"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-semantic-ir-builder within its imported blueprint boundary; require real runtime and external evidence before completion claims."
