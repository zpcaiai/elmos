interface:
  display_name: "Release Train Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-3010-release-train-orchestrator to run FRT-3010 through the governed FRT runtime with exact scope and fail-closed evidence."
