interface:
  display_name: "Semantic IR Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0300-semantic-ir-orchestrator to run FRT-0300 through the governed FRT runtime with exact scope and fail-closed evidence."
