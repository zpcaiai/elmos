interface:
  display_name: "Release Gate Foundation"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0110-release-gate-foundation to run FRT-0110 through the governed FRT runtime with exact scope and fail-closed evidence."
