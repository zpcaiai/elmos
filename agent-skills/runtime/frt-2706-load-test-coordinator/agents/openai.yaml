interface:
  display_name: "Load Test Coordinator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2706-load-test-coordinator to run FRT-2706 through the governed FRT runtime with exact scope and fail-closed evidence."
