schema_version: 1
layout:
  left: [project_tree, breakpoints, learning_mission]
  center: [monaco_editor, execution_marker, inline_values]
  right: [threads, call_stack, scopes, variables, watches, business_context]
  bottom: [debug_console, output, timeline, network, sql, cache, messages, files, locks]
  top: [revision, runtime_profile, environment_badge, safety_mode, session_state, controls, ttl]
linking:
  highlight: [code_symbol, architecture_node, flow_step, data_asset, test_case]
