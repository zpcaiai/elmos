interface:
  display_name: "Graceful Degradation Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2717-graceful-degradation-validator to run FRT-2717 through the governed FRT runtime with exact scope and fail-closed evidence."
