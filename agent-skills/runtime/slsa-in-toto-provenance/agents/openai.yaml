interface:
  display_name: "Slsa In Toto Provenance"
  short_description: "Run slsa-in-toto-provenance with bounded commercial controls"
  default_prompt: "Use $slsa-in-toto-provenance through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
