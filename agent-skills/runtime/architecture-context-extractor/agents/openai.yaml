interface:
  display_name: "Architecture Context Extractor"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $architecture-context-extractor for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
