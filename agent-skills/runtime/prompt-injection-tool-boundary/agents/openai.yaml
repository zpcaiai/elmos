interface:
  display_name: "Prompt Injection Tool Boundary"
  short_description: "Run prompt-injection-tool-boundary with bounded commercial controls"
  default_prompt: "Use $prompt-injection-tool-boundary through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
