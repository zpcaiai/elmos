interface:
  display_name: Budget Planner
  short_description: Run bounded Budget Planner repository capability
  default_prompt: Use $elmos-budget-planner with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
