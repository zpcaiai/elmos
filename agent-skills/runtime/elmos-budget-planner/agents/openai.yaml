interface:
  display_name: "Elmos Budget Planner"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-budget-planner to perform its bounded repository workflow with provenance and fail-closed evidence."
