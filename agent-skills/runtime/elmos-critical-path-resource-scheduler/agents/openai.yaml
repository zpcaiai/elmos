interface:
  display_name: "Elmos Critical Path Resource Scheduler"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-critical-path-resource-scheduler to perform its bounded repository workflow with provenance and fail-closed evidence."
