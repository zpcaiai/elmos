interface:
  display_name: "Post Crash Continuation Recovery"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $post-crash-continuation-recovery for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
