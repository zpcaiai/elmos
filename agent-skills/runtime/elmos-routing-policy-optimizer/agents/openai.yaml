interface:
  display_name: Routing Policy Optimizer
  short_description: Run bounded Routing Policy Optimizer repository capability
  default_prompt: Use $elmos-routing-policy-optimizer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
