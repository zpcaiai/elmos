interface:
  display_name: Model Capability Profiler
  short_description: Run bounded Model Capability Profiler repository capability
  default_prompt: Use $elmos-model-capability-profiler with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
