interface:
  display_name: "elmos-semantic-refinement-counterexample"
  short_description: "Judge target behavior as equality/refinement under the declared relation and emit minimal counter..."
  default_prompt: "Use $elmos-semantic-refinement-counterexample through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-semantic-refinement-counterexample"
  handler_id: "execute_elmos_semantic_refinement_counterexample"
  operation: "SEMANTIC_COMPARISON"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
