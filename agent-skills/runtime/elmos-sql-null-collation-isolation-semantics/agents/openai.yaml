interface:
  display_name: "elmos-sql-null-collation-isolation-semantics"
  short_description: "Capture three-valued logic, collation, locking/isolation, identity/sequence and vendor procedural..."
  default_prompt: "Use $elmos-sql-null-collation-isolation-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-sql-null-collation-isolation-semantics"
  handler_id: "execute_elmos_sql_null_collation_isolation_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
