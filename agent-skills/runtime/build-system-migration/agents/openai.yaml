interface:
  display_name: "Build System Migration"
  short_description: "Run build-system-migration with bounded commercial controls"
  default_prompt: "Use $build-system-migration through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
