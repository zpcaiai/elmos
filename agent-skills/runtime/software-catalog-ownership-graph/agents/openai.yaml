interface:
  display_name: "Software Catalog Ownership Graph"
  short_description: "Run software-catalog-ownership-graph with bounded commercial controls"
  default_prompt: "Use $software-catalog-ownership-graph through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
