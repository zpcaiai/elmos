interface:
  display_name: Change Impact Analyzer
  short_description: Run bounded Change Impact Analyzer repository capability
  default_prompt: Use $elmos-change-impact-analyzer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
