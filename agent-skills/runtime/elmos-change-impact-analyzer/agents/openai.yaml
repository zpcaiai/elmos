interface:
  display_name: "Elmos Change Impact Analyzer"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-change-impact-analyzer to perform its bounded repository workflow with provenance and fail-closed evidence."
