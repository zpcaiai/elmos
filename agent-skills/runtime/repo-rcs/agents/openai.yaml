interface:
  display_name: "Repo Rcs"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $repo-rcs for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
