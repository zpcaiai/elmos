interface:
  display_name: "elmos-model-provider-catalog"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-model-provider-catalog within its imported blueprint boundary; require real runtime and external evidence before completion claims."
