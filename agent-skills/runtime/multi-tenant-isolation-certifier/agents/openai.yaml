interface:
  display_name: "Multi Tenant Isolation Certifier"
  short_description: "Run multi-tenant-isolation-certifier with bounded commercial controls"
  default_prompt: "Use $multi-tenant-isolation-certifier through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
