interface:
  display_name: "ETGB Repository Ingestion Context Validation"
  short_description: "Run ETGB full product assurance with evidence controls"
  default_prompt: "Use $etgb-repository-ingestion-context-validation to run this ETGB capability with fail-closed evidence."
policy:
  allow_implicit_invocation: true
