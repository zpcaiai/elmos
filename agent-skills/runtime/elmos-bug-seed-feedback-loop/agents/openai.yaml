interface:
  display_name: "elmos-bug-seed-feedback-loop"
  short_description: "Feed confirmed minimized failures into route rules, fixture corpora, mutation operators and risk..."
  default_prompt: "Use $elmos-bug-seed-feedback-loop through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-bug-seed-feedback-loop"
  handler_id: "execute_elmos_bug_seed_feedback_loop"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
