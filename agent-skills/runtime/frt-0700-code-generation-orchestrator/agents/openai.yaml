interface:
  display_name: "Code Generation Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0700-code-generation-orchestrator to run FRT-0700 through the governed FRT runtime with exact scope and fail-closed evidence."
