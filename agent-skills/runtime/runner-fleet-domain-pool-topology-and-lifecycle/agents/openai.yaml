interface:
  display_name: "ELMOS Product B36: Runner Fleet Domain Pool Topology And Lifecycle"
  short_description: "Run ELMOS Product B36 contract with fail-closed evidence"
  default_prompt: "Use $runner-fleet-domain-pool-topology-and-lifecycle to implement its fail-closed ELMOS Product B36 contract."
