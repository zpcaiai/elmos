interface:
  display_name: "Source Target Feature Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0503-source-target-feature-mapper to run FRT-0503 through the governed FRT runtime with exact scope and fail-closed evidence."
