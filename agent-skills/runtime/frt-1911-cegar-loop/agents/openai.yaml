interface:
  display_name: "CEGAR Loop"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1911-cegar-loop to run FRT-1911 through the governed FRT runtime with exact scope and fail-closed evidence."
