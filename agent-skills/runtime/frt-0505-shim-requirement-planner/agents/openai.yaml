interface:
  display_name: "Shim Requirement Planner"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0505-shim-requirement-planner to run FRT-0505 through the governed FRT runtime with exact scope and fail-closed evidence."
