interface:
  display_name: "elmos-generics-variance-erasure-semantics"
  short_description: "Preserve variance, bounds, erasure/reification, wildcards and runtime generic metadata across tar..."
  default_prompt: "Use $elmos-generics-variance-erasure-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-generics-variance-erasure-semantics"
  handler_id: "execute_elmos_generics_variance_erasure_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
