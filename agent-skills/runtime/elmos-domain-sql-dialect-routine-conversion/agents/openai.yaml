interface:
  display_name: "SQL Dialect and Routine Conversion Domain Pack"
  short_description: "Run ELMOS-V3-012 with proof-driven controls"
  default_prompt: "Use $elmos-domain-sql-dialect-routine-conversion through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
