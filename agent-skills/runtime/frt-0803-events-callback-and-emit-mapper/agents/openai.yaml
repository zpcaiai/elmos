interface:
  display_name: "Events Callback and Emit Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0803-events-callback-and-emit-mapper to run FRT-0803 through the governed FRT runtime with exact scope and fail-closed evidence."
