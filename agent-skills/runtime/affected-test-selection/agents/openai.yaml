interface:
  display_name: "Affected Test Selection"
  short_description: "Run affected-test-selection with bounded commercial controls"
  default_prompt: "Use $affected-test-selection through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
