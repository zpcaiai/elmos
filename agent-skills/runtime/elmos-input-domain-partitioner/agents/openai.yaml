interface:
  display_name: "elmos-input-domain-partitioner"
  short_description: "Partition valid, invalid, boundary, adversarial and environment-dependent inputs from source cont..."
  default_prompt: "Use $elmos-input-domain-partitioner through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-input-domain-partitioner"
  handler_id: "execute_elmos_input_domain_partitioner"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
