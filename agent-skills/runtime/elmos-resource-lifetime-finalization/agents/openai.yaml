interface:
  display_name: "elmos-resource-lifetime-finalization"
  short_description: "Preserve RAII, try-with-resources, using/defer, GC finalizers and explicit close semantics with f..."
  default_prompt: "Use $elmos-resource-lifetime-finalization through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-resource-lifetime-finalization"
  handler_id: "execute_elmos_resource_lifetime_finalization"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
