interface:
  display_name: "Test Worker Failover"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2617-test-worker-failover to run FRT-2617 through the governed FRT runtime with exact scope and fail-closed evidence."
