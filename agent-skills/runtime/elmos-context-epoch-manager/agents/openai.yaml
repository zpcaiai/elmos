interface:
  display_name: "elmos-context-epoch-manager"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-context-epoch-manager within its imported blueprint boundary; require real runtime and external evidence before completion claims."
