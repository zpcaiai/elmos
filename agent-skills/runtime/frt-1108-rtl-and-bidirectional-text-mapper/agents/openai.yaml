interface:
  display_name: "RTL and Bidirectional Text Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1108-rtl-and-bidirectional-text-mapper to run FRT-1108 through the governed FRT runtime with exact scope and fail-closed evidence."
