interface:
  display_name: "ChinaDB 15 Evidence Ledger Reproducibility"
  short_description: "Run this ChinaDB migration Skill with evidence controls"
  default_prompt: "Use $chinadb-15-evidence-ledger-reproducibility as a SPEC_ONLY ChinaDB migration contract; preserve exact route semantics and fail closed while real evidence is NOT_RUN."
