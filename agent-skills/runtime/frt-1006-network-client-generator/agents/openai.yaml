interface:
  display_name: "Network Client Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1006-network-client-generator to run FRT-1006 through the governed FRT runtime with exact scope and fail-closed evidence."
