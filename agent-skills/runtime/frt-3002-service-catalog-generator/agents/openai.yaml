interface:
  display_name: "Service Catalog Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-3002-service-catalog-generator to run FRT-3002 through the governed FRT runtime with exact scope and fail-closed evidence."
