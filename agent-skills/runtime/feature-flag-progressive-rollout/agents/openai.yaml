interface:
  display_name: "Feature Flag Progressive Rollout"
  short_description: "Run feature-flag-progressive-rollout with bounded commercial controls"
  default_prompt: "Use $feature-flag-progressive-rollout through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
