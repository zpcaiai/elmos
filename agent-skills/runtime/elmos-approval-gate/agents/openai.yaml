interface:
  display_name: "elmos-approval-gate"
  short_description: "Use this imported Elmos commercial blueprint"
  default_prompt: "Use $elmos-approval-gate within its imported blueprint boundary; require real runtime and external evidence before completion claims."
