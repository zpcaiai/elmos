interface:
  display_name: Deterministic Validator
  short_description: Run bounded Deterministic Validator repository capability
  default_prompt: Use $elmos-deterministic-validator with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
