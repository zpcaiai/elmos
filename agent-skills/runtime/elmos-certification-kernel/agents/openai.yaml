interface:
  display_name: "Certification Kernel"
  short_description: "Run ELMOS-V3-008 with proof-driven controls"
  default_prompt: "Use $elmos-certification-kernel through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
