interface:
  display_name: "Transformation Explainability Ledger"
  short_description: "Run transformation-explainability-ledger with bounded commercial controls"
  default_prompt: "Use $transformation-explainability-ledger through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
