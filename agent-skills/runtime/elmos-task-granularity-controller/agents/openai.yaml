interface:
  display_name: "Elmos Task Granularity Controller"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-task-granularity-controller to perform its bounded repository workflow with provenance and fail-closed evidence."
