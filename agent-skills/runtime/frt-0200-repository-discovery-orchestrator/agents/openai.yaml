interface:
  display_name: "Repository Discovery Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0200-repository-discovery-orchestrator to run FRT-0200 through the governed FRT runtime with exact scope and fail-closed evidence."
