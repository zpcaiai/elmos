interface:
  display_name: "Target Architecture Orchestrator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0600-target-architecture-orchestrator to run FRT-0600 through the governed FRT runtime with exact scope and fail-closed evidence."
