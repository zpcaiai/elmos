interface:
  display_name: "Semantic Drift Detector"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1916-semantic-drift-detector to run FRT-1916 through the governed FRT runtime with exact scope and fail-closed evidence."
