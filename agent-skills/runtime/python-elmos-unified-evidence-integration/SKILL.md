---
name: python-elmos-unified-evidence-integration
description: Map Python environment, semantic, Web, pipeline, numerical, and model evidence into shared ELMOS risks, steps, costs, audit, portfolio, delivery, and evidence-pack contracts. Use for Python control-plane integration, evidence extensions, cross-engine portfolios, reporting, billing, or delivery.
---

# Python and ELMOS Unified Evidence Integration

## Reuse shared authority

Reuse Repository Snapshot, Assessment, Migration Plan/Step, Execution Attempt, Patch Segment, Validation Result, Risk Finding, Evidence, Delivery Snapshot, Cost, Audit, SCM, rollback, and human acceptance. Preserve Python detail only in a versioned `elmos.python-evidence.v1` extension with a content-addressed artifact reference.

Map Python findings to stable shared categories without discarding the original code. Map packaging/framework/pipeline/model steps to shared build, framework, data-pipeline, model-runtime, and behavior-validation types.

Use executors `LIBCST`, `PYTHON_AST`, `UV`, `PIP`, `CONDA`, `MYPY`, `PYRIGHT`, `PYTEST`, `CODING_AGENT`, `DATA_VALIDATOR`, `MODEL_VALIDATOR`, and `HUMAN`. Meter scan, environment resolution, Codemods, CPU/GPU Runner time, Agent repair, data validation, and model validation through the shared usage ledger.

## Build one portfolio

Place Java services, .NET services, Python Web apps, Python pipelines, and Python model services in the same tenant portfolio. Model HTTP, message, data, model, shared database, file, and package edges. Reject cross-tenant plans and unknown edge endpoints.

Extend PR/MR and Evidence Pack views with Python target, lock, framework, data contract, numerical/model decisions, hardware, and remaining risks. Do not let a renderer upgrade `NOT_RUN`, `MISSING`, `INCONCLUSIVE`, or `BLOCKED`.

Accept only when the control plane remains ignorant of LibCST internals, common reports render Python facts, extension detail stays lossless, costs/audit share existing authority, and delivery reuses signed Evidence and rollback gates.

