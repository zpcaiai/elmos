interface:
  display_name: "Product Decision Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0504-product-decision-generator to run FRT-0504 through the governed FRT runtime with exact scope and fail-closed evidence."
