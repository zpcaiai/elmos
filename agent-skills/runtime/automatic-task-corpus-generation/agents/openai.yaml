interface:
  display_name: "Automatic Task Corpus Generation"
  short_description: "Run automatic-task-corpus-generation with bounded commercial controls"
  default_prompt: "Use $automatic-task-corpus-generation through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
