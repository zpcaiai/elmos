interface:
  display_name: "Reproducible Build Verifier"
  short_description: "Run reproducible-build-verifier with bounded commercial controls"
  default_prompt: "Use $reproducible-build-verifier through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
