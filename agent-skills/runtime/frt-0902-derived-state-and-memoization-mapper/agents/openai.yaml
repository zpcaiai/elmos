interface:
  display_name: "Derived State and Memoization Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0902-derived-state-and-memoization-mapper to run FRT-0902 through the governed FRT runtime with exact scope and fail-closed evidence."
