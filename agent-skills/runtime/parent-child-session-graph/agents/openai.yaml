interface:
  display_name: "Parent Child Session Graph"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $parent-child-session-graph for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
