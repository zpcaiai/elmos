interface:
  display_name: "Parse Failure Evidence"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $parse-failure-evidence for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
