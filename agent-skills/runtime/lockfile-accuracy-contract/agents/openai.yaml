interface:
  display_name: "Lockfile Accuracy Contract"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $lockfile-accuracy-contract for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
