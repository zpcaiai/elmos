interface:
  display_name: "elmos-verified-lowering-route"
  short_description: "Define high-assurance lowering paths where a verified compiler/intermediate target or proof-produ..."
  default_prompt: "Use $elmos-verified-lowering-route through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-verified-lowering-route"
  handler_id: "execute_elmos_verified_lowering_route"
  operation: "FORMAL_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
