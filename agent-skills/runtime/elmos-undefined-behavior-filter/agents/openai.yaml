interface:
  display_name: "elmos-undefined-behavior-filter"
  short_description: "Exclude or explicitly model source cases whose behavior is undefined/unspecified before treating..."
  default_prompt: "Use $elmos-undefined-behavior-filter through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-undefined-behavior-filter"
  handler_id: "execute_elmos_undefined_behavior_filter"
  operation: "FUZZ_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
