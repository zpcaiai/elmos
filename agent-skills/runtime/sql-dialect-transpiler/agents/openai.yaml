interface:
  display_name: "Sql Dialect Transpiler"
  short_description: "Run sql-dialect-transpiler with bounded commercial controls"
  default_prompt: "Use $sql-dialect-transpiler through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
