interface:
  display_name: "Region Failure Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2811-region-failure-validator to run FRT-2811 through the governed FRT runtime with exact scope and fail-closed evidence."
