interface:
  display_name: Context Slicer
  short_description: Run bounded Context Slicer repository capability
  default_prompt: Use $elmos-context-slicer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
