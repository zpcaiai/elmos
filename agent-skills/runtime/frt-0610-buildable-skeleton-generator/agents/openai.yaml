interface:
  display_name: "Buildable Skeleton Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0610-buildable-skeleton-generator to run FRT-0610 through the governed FRT runtime with exact scope and fail-closed evidence."
