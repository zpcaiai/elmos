interface:
  display_name: "Continuous Fuzz Certification"
  short_description: "Run continuous-fuzz-certification with bounded commercial controls"
  default_prompt: "Use $continuous-fuzz-certification through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
