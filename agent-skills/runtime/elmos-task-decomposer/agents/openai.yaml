interface:
  display_name: Task Decomposer
  short_description: Run bounded Task Decomposer repository capability
  default_prompt: Use $elmos-task-decomposer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
