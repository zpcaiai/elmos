interface:
  display_name: "Input Validation and Injection Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2908-input-validation-and-injection-validator to run FRT-2908 through the governed FRT runtime with exact scope and fail-closed evidence."
