interface:
  display_name: "Semantic IR Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0312-semantic-ir-validator to run FRT-0312 through the governed FRT runtime with exact scope and fail-closed evidence."
