interface:
  display_name: "Spring Db Diff"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $spring-db-diff for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
