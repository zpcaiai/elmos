interface:
  display_name: "ETGB Api Sdk Webhook Validation"
  short_description: "Run ETGB full product assurance with evidence controls"
  default_prompt: "Use $etgb-api-sdk-webhook-validation to run this ETGB capability with fail-closed evidence."
policy:
  allow_implicit_invocation: true
