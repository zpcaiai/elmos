interface:
  display_name: "Spring Repo Graph"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $spring-repo-graph for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
