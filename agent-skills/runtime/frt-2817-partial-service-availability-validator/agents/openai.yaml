interface:
  display_name: "Partial-Service Availability Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2817-partial-service-availability-validator to run FRT-2817 through the governed FRT runtime with exact scope and fail-closed evidence."
