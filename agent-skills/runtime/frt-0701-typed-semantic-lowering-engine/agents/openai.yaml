interface:
  display_name: "Typed Semantic Lowering Engine"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0701-typed-semantic-lowering-engine to run FRT-0701 through the governed FRT runtime with exact scope and fail-closed evidence."
