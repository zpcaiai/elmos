interface:
  display_name: Worktree Manager
  short_description: Run bounded Worktree Manager repository capability
  default_prompt: Use $elmos-worktree-manager with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
