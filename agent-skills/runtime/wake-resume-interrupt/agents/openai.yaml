interface:
  display_name: "Wake Resume Interrupt"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $wake-resume-interrupt for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
