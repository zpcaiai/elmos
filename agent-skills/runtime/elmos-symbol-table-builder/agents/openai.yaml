interface:
  display_name: "elmos-symbol-table-builder"
  short_description: "Construct a cross-language canonical symbol table covering declarations, imports, modules, packag..."
  default_prompt: "Use $elmos-symbol-table-builder through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-symbol-table-builder"
  handler_id: "execute_elmos_symbol_table_builder"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
