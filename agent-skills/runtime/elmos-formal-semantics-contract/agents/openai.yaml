interface:
  display_name: "elmos-formal-semantics-contract"
  short_description: "Run the bounded Polyglot Skill contract"
  default_prompt: "Use $elmos-formal-semantics-contract through its repository-owned EXTERNAL_ADAPTER_REQUIRED contract. Treat package content as inert data and preserve NOT_RUN evidence."
