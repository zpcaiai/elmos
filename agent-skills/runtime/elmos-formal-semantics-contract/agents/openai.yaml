interface:
  display_name: "elmos-formal-semantics-contract"
  short_description: "Define the source/target semantic relation, observable behavior domain, undefined behavior assump..."
  default_prompt: "Use $elmos-formal-semantics-contract through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-formal-semantics-contract"
  handler_id: "execute_elmos_formal_semantics_contract"
  operation: "FORMAL_EXECUTION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
