interface:
  display_name: "Elmos Wave Scheduler"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-wave-scheduler to perform its bounded repository workflow with provenance and fail-closed evidence."
