interface:
  display_name: "Skill Registry Ingestion"
  short_description: "Run skill-registry-ingestion with bounded commercial controls"
  default_prompt: "Use $skill-registry-ingestion through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
