interface:
  display_name: "Elmos Integration Manager"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-integration-manager to perform its bounded repository workflow with provenance and fail-closed evidence."
