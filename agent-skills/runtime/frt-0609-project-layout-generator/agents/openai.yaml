interface:
  display_name: "Project Layout Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0609-project-layout-generator to run FRT-0609 through the governed FRT runtime with exact scope and fail-closed evidence."
