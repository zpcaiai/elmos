interface:
  display_name: "Harness Runtime Kernel"
  short_description: "Run ELMOS-V3-007 with proof-driven controls"
  default_prompt: "Use $elmos-harness-runtime-kernel through the proof-driven harness runtime with exact source identity, least privilege, replayable evidence, and fail-closed gates."
policy:
  allow_implicit_invocation: true
