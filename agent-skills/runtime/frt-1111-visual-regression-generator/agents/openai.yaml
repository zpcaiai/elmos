interface:
  display_name: "Visual Regression Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1111-visual-regression-generator to run FRT-1111 through the governed FRT runtime with exact scope and fail-closed evidence."
