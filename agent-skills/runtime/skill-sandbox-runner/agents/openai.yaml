interface:
  display_name: "Skill Sandbox Runner"
  short_description: "Run skill-sandbox-runner with bounded commercial controls"
  default_prompt: "Use $skill-sandbox-runner through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
