interface:
  display_name: "Workload Model Registry"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2701-workload-model-registry to run FRT-2701 through the governed FRT runtime with exact scope and fail-closed evidence."
