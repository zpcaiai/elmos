interface:
  display_name: "Elmos Architecture Indexer"
  short_description: "Run repository task routing with bounded evidence"
  default_prompt: "Use $elmos-architecture-indexer to perform its bounded repository workflow with provenance and fail-closed evidence."
