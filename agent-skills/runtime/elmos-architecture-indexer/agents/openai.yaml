interface:
  display_name: Architecture Indexer
  short_description: Run bounded Architecture Indexer repository capability
  default_prompt: Use $elmos-architecture-indexer with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
