interface:
  display_name: "Idempotency Under Load Validator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-2704-idempotency-under-load-validator to run FRT-2704 through the governed FRT runtime with exact scope and fail-closed evidence."
