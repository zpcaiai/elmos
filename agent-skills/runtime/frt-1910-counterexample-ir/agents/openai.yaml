interface:
  display_name: "Counterexample IR"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1910-counterexample-ir to run FRT-1910 through the governed FRT runtime with exact scope and fail-closed evidence."
