interface:
  display_name: "Successful Patch to Recipe Learning"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $successful-patch-to-recipe-learning for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
