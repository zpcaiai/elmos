interface:
  display_name: "Build Toolchain Detector"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0203-build-toolchain-detector to run FRT-0203 through the governed FRT runtime with exact scope and fail-closed evidence."
