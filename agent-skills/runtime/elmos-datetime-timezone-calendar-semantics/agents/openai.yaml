interface:
  display_name: "elmos-datetime-timezone-calendar-semantics"
  short_description: "Preserve instant/local date/time, timezone database, DST ambiguity, calendar rules, epoch ranges..."
  default_prompt: "Use $elmos-datetime-timezone-calendar-semantics through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-datetime-timezone-calendar-semantics"
  handler_id: "execute_elmos_datetime_timezone_calendar_semantics"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
