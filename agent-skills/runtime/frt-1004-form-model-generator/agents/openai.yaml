interface:
  display_name: "Form Model Generator"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-1004-form-model-generator to run FRT-1004 through the governed FRT runtime with exact scope and fail-closed evidence."
