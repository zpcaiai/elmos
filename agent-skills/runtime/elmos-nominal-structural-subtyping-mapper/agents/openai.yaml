interface:
  display_name: "elmos-nominal-structural-subtyping-mapper"
  short_description: "Preserve inheritance, interfaces, protocols, duck typing and structural compatibility across diff..."
  default_prompt: "Use $elmos-nominal-structural-subtyping-mapper through its exact repository-owned semantic-assurance runtime binding; preserve NOT_RUN and blockers."
runtime:
  module: "elmos_semantic_assurance.registry"
  registry: "SkillRegistry"
  registry_key: "elmos-nominal-structural-subtyping-mapper"
  handler_id: "execute_elmos_nominal_structural_subtyping_mapper"
  operation: "MODEL_NORMALIZATION"
  generic_fallback: false
security:
  trusted_scope_required: true
  archive_code_execution: "forbidden"
  external_effects: "not-authorized-by-skill"
evidence:
  external: "NOT_RUN"
  certification: "NOT_CERTIFIED"
