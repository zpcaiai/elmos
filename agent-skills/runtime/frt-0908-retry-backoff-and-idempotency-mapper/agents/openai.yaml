interface:
  display_name: "Retry Backoff and Idempotency Mapper"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0908-retry-backoff-and-idempotency-mapper to run FRT-0908 through the governed FRT runtime with exact scope and fail-closed evidence."
