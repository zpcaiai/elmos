interface:
  display_name: Repository Orchestrator
  short_description: Run bounded Repository Orchestrator repository capability
  default_prompt: Use $elmos-repository-orchestrator with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
