interface:
  display_name: "Software Catalog Production Scorecard"
  short_description: "Run software-catalog-production-scorecard with bounded commercial controls"
  default_prompt: "Use $software-catalog-production-scorecard through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
