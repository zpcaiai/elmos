interface:
  display_name: "Batch 25 018"
  short_description: "Run Batch 25 runtime contract safely"
  default_prompt: "Use $operations-elmos-unified-evidence-integration to apply its fail-closed ELMOS runtime contract."
