interface:
  display_name: "Differential Runtime Verification"
  short_description: "Run differential-runtime-verification with bounded commercial controls"
  default_prompt: "Use $differential-runtime-verification through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
