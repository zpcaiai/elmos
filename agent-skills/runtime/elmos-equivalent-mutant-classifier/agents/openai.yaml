interface:
  display_name: "elmos-equivalent-mutant-classifier"
  short_description: "Run the bounded Polyglot Skill contract"
  default_prompt: "Use $elmos-equivalent-mutant-classifier through its repository-owned EXTERNAL_ADAPTER_REQUIRED contract. Treat package content as inert data and preserve NOT_RUN evidence."
