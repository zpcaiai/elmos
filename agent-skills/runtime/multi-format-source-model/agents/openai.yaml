interface:
  display_name: "Multi Format Source Model"
  short_description: "Run this Spring Golden Route v2 Skill safely"
  default_prompt: "Use $multi-format-source-model for its exact imported contract; keep runtime evidence NOT_RUN unless independently executed."
