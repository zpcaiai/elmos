interface:
  display_name: Worker Prompt Builder
  short_description: Run bounded Worker Prompt Builder repository capability
  default_prompt: Use $elmos-worker-prompt-builder with trusted scope and explicit inputs; report implementation prerequisites and preserve evidence boundaries.
