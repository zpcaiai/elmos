interface:
  display_name: "Metamorphic Equivalence Testing"
  short_description: "Run metamorphic-equivalence-testing with bounded commercial controls"
  default_prompt: "Use $metamorphic-equivalence-testing through the authenticated public CommercialCapabilityExpansionService.execute surface with exact scope; private handler resolution is runtime-owned and evidence fails closed."
policy:
  allow_implicit_invocation: true
