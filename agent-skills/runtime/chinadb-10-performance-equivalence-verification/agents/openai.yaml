interface:
  display_name: "ChinaDB 10 Performance Equivalence Verification"
  short_description: "Run this ChinaDB migration Skill with evidence controls"
  default_prompt: "Use $chinadb-10-performance-equivalence-verification as a SPEC_ONLY ChinaDB migration contract; preserve exact route semantics and fail closed while real evidence is NOT_RUN."
