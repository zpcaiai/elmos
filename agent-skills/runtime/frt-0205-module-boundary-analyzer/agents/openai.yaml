interface:
  display_name: "Module Boundary Analyzer"
  short_description: "Run this FRT frontend Skill with evidence controls"
  default_prompt: "Use $frt-0205-module-boundary-analyzer to run FRT-0205 through the governed FRT runtime with exact scope and fail-closed evidence."
