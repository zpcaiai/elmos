{
  "certification": "NOT_CERTIFIED",
  "note": "This checked-in marker is intentionally not a receipt and can never satisfy spring-launch-gate. Produce real external evidence outside the repository and validate it against the referenced schemas and runbook.",
  "receipt_schema": "schemas/batch30/spring-launch-external-evidence.schema.json",
  "status": "NOT_RUN",
  "trust_store_schema": "schemas/batch30/spring-launch-trust-store.schema.json"
}
