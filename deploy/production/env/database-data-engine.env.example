# Database-data worker transport budgets only; no database credential is needed
# by the source-side typed SQL preflight route.
ELMOS_CHINADB_SQL_PREFLIGHT_CONNECT_TIMEOUT=2s
ELMOS_CHINADB_SQL_PREFLIGHT_REQUEST_TIMEOUT=15s
