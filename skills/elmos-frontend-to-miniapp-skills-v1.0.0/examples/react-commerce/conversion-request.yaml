request_id: conv-react-commerce
tenant_id: fixture
source:
  root: ./source
  revision: fixture-react-commerce-v1
  framework_hint: react
  include: ["src/**/*", "package.json"]
  exclude: []
targets: [wechat, alipay]
strategy:
  priority: platform-native
  webview_fallback: deny
  full_page_canvas_fallback: deny
  unsupported_policy: ask-decision
quality:
  critical_flow_pass_rate: 1.0
  visual_similarity_min: 0.95
  max_auto_repair_iterations: 3
release:
  mode: build-only
  human_approval_required: true
  credential_refs: {}
