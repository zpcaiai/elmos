# Golden Execution Route v3.2

User Intent → Repository Discovery → Semantic Compiler → Transformation Program → WorkPackage DAG → Finalized ExecutionPlan → Authority Check → Capability Negotiation → CapabilityLease → Agent Runtime → Isolated Worker → Managed Worktree → Candidate Result → Result Intercept → RepositoryCheckpoint → ResultFence → VerificationTicket → Truth Runner → Evidence/Provenance → OPA Gate → Certifier → CertifiedCheckpoint → PublicationBroker → DeploymentBroker → Post-publication exact-artifact verification → CertifiedReleaseManifest.
