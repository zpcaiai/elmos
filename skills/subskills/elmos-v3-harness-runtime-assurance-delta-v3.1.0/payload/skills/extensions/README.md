# v3.1 Kernel Extensions

These 13 Skills refine existing v3 Kernels. They are deliberately placed under `skills/extensions/` so the original `skills/P0|P1` routable registry remains exactly 16 entries. The Workflow Compiler may invoke an extension only through its declared canonical owner Kernel.
