apiVersion: elmos.ai/v2alpha1
kind: Skill
metadata:
  name: elmos-sql-query-equivalence
  title: SQL Query Equivalence
  version: 1.0.0
  packageId: elmos-formal-assurance-kernel-v1.0.0
  priority: P0
  domain: sql-conversion
  owners:
  - formal-assurance-platform
  - domain-owner
  labels:
    commercial: production-contract
    failClosed: 'true'
    tenantScoped: 'true'
spec:
  summary: 在 PK/FK/UNIQUE/CHECK/NOT NULL 等约束下证明源、目标查询的 Bag、Set 或有序结果等价。
  dependencies:
  - elmos-observable-behavior-contract
  - elmos-proof-obligation-planner
  - elmos-verifier-portfolio-router
  - elmos-sql-semantic-ir
  capabilities:
  - unbounded query equivalence
  - SMT-based bounded checking
  - relational algebra rewrites
  - counterexample database synthesis
  inputs:
  - name: source query
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  - name: target query
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  - name: schema constraints
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  - name: normalization policy
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  outputs:
  - name: equivalence result
    schemaRef: contracts/schemas/proof-artifact.schema.json
  - name: proof certificate/log
    schemaRef: contracts/schemas/proof-artifact.schema.json
  - name: counterexample DB
    schemaRef: contracts/schemas/proof-artifact.schema.json
  proofStatuses:
  - PROVED_CERTIFIED
  - PROVED_INDUCTIVE
  - PROVED_SOLVER_TRUSTED
  - PROVED_FOR_SUPPORTED_FRAGMENT
  - BOUNDED_NO_COUNTEREXAMPLE
  - REFUTED_WITH_COUNTEREXAMPLE
  - UNKNOWN_TIMEOUT
  - UNKNOWN_RESOURCE_LIMIT
  - UNSUPPORTED
  - ASSUMPTION_REQUIRED
  - RUNTIME_MONITORED
  - WAIVED_BY_APPROVER
  runtime:
    executionMode: durable-asynchronous
    sandbox: secretless-deny-network
    idempotencyRequired: true
    checkpointing: true
    machineEtaUnit: wall-clock-seconds
    accountConcurrencyLimit: 3
  security:
    classification: tenant-confidential
    crossTenantCache: deny-by-default
    evidenceIntegrity: sha256-content-addressed
    adapterFailureMode: fail-closed
  releaseGates:
  - P05_DEPLOYMENT_COMPLETE
  - E1_STATIC
  - E2_MODEL
  - E3_DIFFERENTIAL
  - E4_FAILURE_INJECTION
  - E5_CUSTOMER_GOLDEN_ROUTE
