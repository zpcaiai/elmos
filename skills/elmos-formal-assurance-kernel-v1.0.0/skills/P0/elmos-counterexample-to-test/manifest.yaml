apiVersion: elmos.ai/v2alpha1
kind: Skill
metadata:
  name: elmos-counterexample-to-test
  title: Counterexample-to-Test Compiler
  version: 1.0.0
  packageId: elmos-formal-assurance-kernel-v1.0.0
  priority: P0
  domain: platform
  owners:
  - formal-assurance-platform
  - domain-owner
  labels:
    commercial: production-contract
    failClosed: 'true'
    tenantScoped: 'true'
spec:
  summary: 把 SMT 模型、TLA+ Trace、Alloy Instance、SQL 反例数据库和符号执行路径最小化并生成多语言回归测试。
  dependencies:
  - elmos-proof-obligation-planner
  - elmos-proof-status-policy
  - elmos-verifier-portfolio-router
  - elmos-formal-assurance-orchestrator
  capabilities:
  - counterexample minimization
  - trace concretization
  - fixture synthesis
  - test deduplication
  inputs:
  - name: verifier counterexamples
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  - name: source maps
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  - name: test framework profile
    required: true
    schemaRef: contracts/schemas/formal-spec.schema.json
  outputs:
  - name: JUnit/pytest/xUnit/Go/Rust/SQL/Scenario tests
    schemaRef: contracts/schemas/proof-artifact.schema.json
  - name: replay metadata
    schemaRef: contracts/schemas/proof-artifact.schema.json
  proofStatuses:
  - PROVED_CERTIFIED
  - PROVED_INDUCTIVE
  - PROVED_SOLVER_TRUSTED
  - PROVED_FOR_SUPPORTED_FRAGMENT
  - BOUNDED_NO_COUNTEREXAMPLE
  - REFUTED_WITH_COUNTEREXAMPLE
  - UNKNOWN_TIMEOUT
  - UNKNOWN_RESOURCE_LIMIT
  - UNSUPPORTED
  - ASSUMPTION_REQUIRED
  - RUNTIME_MONITORED
  - WAIVED_BY_APPROVER
  runtime:
    executionMode: durable-asynchronous
    sandbox: secretless-deny-network
    idempotencyRequired: true
    checkpointing: true
    machineEtaUnit: wall-clock-seconds
    accountConcurrencyLimit: 3
  security:
    classification: tenant-confidential
    crossTenantCache: deny-by-default
    evidenceIntegrity: sha256-content-addressed
    adapterFailureMode: fail-closed
  releaseGates:
  - P05_DEPLOYMENT_COMPLETE
  - E1_STATIC
  - E2_MODEL
  - E3_DIFFERENTIAL
  - E4_FAILURE_INJECTION
  - E5_CUSTOMER_GOLDEN_ROUTE
