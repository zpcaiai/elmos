# Policy-as-code

OPA/Rego modules enforce proof-status honesty, release gates, waivers, tenant isolation, artifact integrity and solver sandboxing. Run `opa test policies/rego -v` in a release environment.
