{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "elmos-formal-assurance.fullname" .) .Values.serviceAccount.name }}
automountServiceAccountToken: false
{{- end }}
