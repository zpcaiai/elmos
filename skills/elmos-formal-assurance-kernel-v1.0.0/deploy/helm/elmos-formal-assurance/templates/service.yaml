apiVersion: v1
kind: Service
metadata:
  name: {{ include "elmos-formal-assurance.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: {app.kubernetes.io/name: elmos-formal-assurance}
  ports:
    - {name: http, port: {{ .Values.service.port }}, targetPort: http, protocol: TCP}
