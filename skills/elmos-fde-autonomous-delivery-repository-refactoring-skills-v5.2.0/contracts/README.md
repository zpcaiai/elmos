# Typed contracts

All schemas use JSON Schema Draft 2020-12. Examples are validated by `scripts/validate_json_schemas.py`. These contracts are package-level semantics and must be mapped to the existing Elmos canonical contracts rather than installed as a second source of truth.
