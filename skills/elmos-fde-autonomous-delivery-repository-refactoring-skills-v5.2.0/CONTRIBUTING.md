# Contributing

Add or modify a capability only after proving existing ownership cannot absorb it. Every change needs typed contracts, positive/negative trigger cases, authority/effect behavior, rollback, telemetry, tests, traceability and an honest completion boundary. Run `./validate.sh --strict`.
