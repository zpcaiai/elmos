# Package map

- `docs/` contains product and architecture decisions.
- `catalog/` contains requirements, scenarios, Skills, Adapters, routes, batches and tasks.
- `skills/atomic/` contains implementation contracts.
- `contracts/` contains schemas and API/event contracts.
- `evals/` contains trigger and product evaluation data.
- `reference/` contains executable semantics, not a production service.
