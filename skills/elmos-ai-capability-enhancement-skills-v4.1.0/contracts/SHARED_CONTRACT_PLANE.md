# Shared Contract Plane

This directory is copied byte-for-byte from `elmos-ai-native-project-factory-total-skills-v4.0.0` into both split packages so capability evidence and certification decisions use the same schemas.
