openapi: 3.2.0
info:
  title: QA and Certification API
  version: 4.0.0
servers:
- url: https://RELEASE_TIME_CONTROL_PLANE
paths:
  /v1/qa/plans:
    post:
      operationId: v1_qa_plans_start
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunRequest'
      responses:
        '202':
          description: Accepted
        '403':
          description: Denied
        '409':
          description: Conflict
        '422':
          description: Invalid contract
    get:
      operationId: v1_qa_plans_list
      responses:
        '200':
          description: Typed, tenant-scoped results
  /v1/certification/requests:
    post:
      operationId: v1_certification_requests_start
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunRequest'
      responses:
        '202':
          description: Accepted
        '403':
          description: Denied
        '409':
          description: Conflict
        '422':
          description: Invalid contract
    get:
      operationId: v1_certification_requests_list
      responses:
        '200':
          description: Typed, tenant-scoped results
components:
  securitySchemes:
    oidc:
      type: openIdConnect
      openIdConnectUrl: https://RELEASE_TIME_IDP/.well-known/openid-configuration
  schemas:
    RunRequest:
      type: object
      required:
      - goal_id
      - revision_set_id
      properties:
        goal_id:
          type: string
        revision_set_id:
          type: string
        idempotency_key:
          type: string
security:
- oidc: []
