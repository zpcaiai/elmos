# Database Security and Routine Extraction

Database routes must preserve roles, grants, row/column policy, masking, auditing and privileged routine semantics. Routine-to-service extraction is dual-run and explicitly models transaction, lock, retry and side effects.
