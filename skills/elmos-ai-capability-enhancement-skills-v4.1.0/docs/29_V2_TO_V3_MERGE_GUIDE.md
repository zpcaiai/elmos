# v2.0.0 → v3.0.0 Merge Guide

v3 preserves all 130 v2 Skills and 86 adapters, then adds 70 non-routable certification components and 39 adapters without changing the 16 canonical v3 entry points. Existing project-generation ownership remains unchanged. Install v3 over v2 with the installer backup mechanism, validate receipts, run migrations 008–013 and enable new workflows behind release gates.
