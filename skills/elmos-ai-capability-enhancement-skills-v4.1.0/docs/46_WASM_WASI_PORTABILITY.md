# Wasm Component and WASI Portability

WIT and the Component Model are treated as typed interface and resource-lifecycle contracts. WASI capabilities remain deny-by-default and are verified natively across selected runtimes. No component or sandbox conformance is claimed from manifest generation alone.
