# Model Artifact Provenance

Weights, tokenizer, adapters, merges, conversions, quantization, licenses, signatures and serving images form one immutable artifact graph. Provider aliases and filenames are not provenance.
