# Protocol Interoperability

MCP, A2A, ACP, ChatGPT Apps, OpenAPI, gRPC, GraphQL, CloudEvents and AsyncAPI are represented by protocol-specific profiles connected through common Tool, Agent, Interaction and Evidence IR. Bridges preserve identity, cancellation, streaming, side-effect and error semantics; unsupported behavior is recorded rather than silently adapted.
