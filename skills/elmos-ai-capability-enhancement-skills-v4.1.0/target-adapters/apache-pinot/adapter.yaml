apiVersion: elmos.ai/v3alpha1
kind: TargetAdapter
metadata:
  name: apache-pinot
  ecosystem: Apache Pinot
  version: 4.0.0
spec:
  adapterKind: realtime-analytics
  maturity: P1
  modes:
  - schema
  - table
  - ingestion
  - query
  interfaces:
  - detect
  - profile
  - import
  - lower
  - emit
  - native_validate
  - upgrade
  - evidence
  versionPolicy:
    discovery: official metadata, source lockfile or exact customer environment
    pin: exact version and artifact digest at release
    evidenceInvalidation:
    - API/DSL/schema/protocol change
    - runtime/plugin change
    - security advisory or signer revocation
    - conformance suite change
  authority:
    ambientFilesystem: false
    ambientNetwork: false
    ambientSecrets: false
    requestScoped: true
    failureMode: fail-closed
  artifacts:
  - apache-pinot exact-version profile
  - apache-pinot capability and semantic-gap map
  - apache-pinot native conformance evidence
  - apache-pinot deployment/rollback profile
  releaseBoundary: adapter package presence is not native conformance
