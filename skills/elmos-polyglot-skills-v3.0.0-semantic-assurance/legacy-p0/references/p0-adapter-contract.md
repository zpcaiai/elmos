# P0 adapter contract

Every adapter must identify dialect/version, parse with source spans, resolve names where feasible, lower to shared IRs, surface unsupported constructs, emit route obligations, invoke native verification and attach evidence. Syntax-only parsing can support discovery but cannot alone support production conversion claims.
