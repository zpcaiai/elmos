# Observable Behavior Model

Observable behavior can include return values, exceptions, committed DB state, files, messages/events, external network calls, user-visible UI state, authorization outcomes, logs when contractual, exit codes, ordering/timing when contractual, performance budgets and resource usage. Internal structure may differ if the declared observational relation still holds.
