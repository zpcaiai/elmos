# Frontend Fidelity Standard

Use at least two sources of truth where practical: lossless CST plus native compiler/language-service semantic frontend. Preserve dialect, preprocessing, source coordinates, comments/directives, symbol bindings and error-recovery confidence. Parser recovery never authorizes transformation of unknown semantics.
