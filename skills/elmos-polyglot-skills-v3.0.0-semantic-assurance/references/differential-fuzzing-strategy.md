# Differential & Metamorphic Fuzzing Strategy

Use grammar/type constrained generators to avoid meaningless invalid programs; exclude or classify UB; compare source and target with multi-oracle differential execution; add metamorphic transformations and property-based stateful tests; run compiler/runtime N-version matrices; minimize failures; promote every confirmed mismatch into permanent regression corpus.
