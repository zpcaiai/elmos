# Examples

- `java-spring-to-csharp-job.yaml`: bounded conversion job.
- `spring-modernization-job.yaml`: same-stack Spring upgrade.
- `project-generation-spec.yaml`: complete product-generation input.
- `behavior-contract.json`: a sample behavior contract.
- `target-profile.yaml`: target architecture example.

Examples are inputs and schemas only. Their readiness is `not-run`.
