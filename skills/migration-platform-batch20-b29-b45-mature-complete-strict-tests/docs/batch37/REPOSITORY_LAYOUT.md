# Repository layout

Use `.agents/skills/b37-*`, `schemas/batch37`, `templates/batch37`, `scripts/batch37`, `tests/batch37`, and `marketplace-packs/<pack-key>`. Extension source packages should live outside control-plane internals and compile only against published SDKs.
