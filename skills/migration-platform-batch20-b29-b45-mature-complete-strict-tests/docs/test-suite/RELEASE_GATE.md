# Release Gate

The authoritative gate validates catalog and coverage, loads actual case results, verifies evidence manifests and digests, rejects P0/P1 failure/not-run, rejects stale or missing evidence, applies strict thresholds, validates waiver policy and produces a signed-ready release decision artifact. Editing the decision file cannot create a pass.
