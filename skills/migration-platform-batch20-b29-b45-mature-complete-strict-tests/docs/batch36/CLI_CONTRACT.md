# CLI contract

Commands use stable nouns and verbs, versioned machine-readable output, explicit tenant/project/environment context, deterministic exit codes, idempotency, dry-run, noninteractive safety, secure credentials, cancellation, resume, and deprecation policies.
