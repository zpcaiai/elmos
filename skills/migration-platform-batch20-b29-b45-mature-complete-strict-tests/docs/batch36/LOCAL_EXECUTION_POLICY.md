# Local execution policy

Local preview and evaluation use pinned tools and artifacts, isolated ephemeral workspaces, deny-by-default network, tenant-scoped caches, no production secrets, reproducible manifests, bounded resources, cleanup evidence, and parity checks with certified runners.
