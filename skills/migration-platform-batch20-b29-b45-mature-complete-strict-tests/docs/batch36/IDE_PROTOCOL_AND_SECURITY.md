# IDE protocol and security

Use typed versioned RPC with request IDs, cancellation, deadlines, document versions, artifact digests, tenant/project scope, structured arguments, allowlisted tools and paths, and explicit permissions. Do not expose arbitrary shell, filesystem, network, secret, or repository-write access.
