# Implementation contract

Every Developer Experience Pack is exact, versioned, repository-scoped, migration-route-scoped, host/provider-scoped, policy-bound, and evidence-backed. IDEs, CLI, bots, and web surfaces must consume the same typed protocol, source maps, ownership, policy, artifact, review, and evidence contracts. No surface may add a privileged backdoor or independent certification path.
