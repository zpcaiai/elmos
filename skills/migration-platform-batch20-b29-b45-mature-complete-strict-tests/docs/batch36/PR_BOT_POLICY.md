# PR bot policy

SCM bots use minimum permissions, signed webhooks, replay protection, exact commit SHA checks, fork isolation, one updatable summary, scoped commands, rate limits, no self-approval, no secret access for untrusted code, and evidence-linked status checks.
