# 使用示例

## Codex

```text
$elmos-debug-adapter-gateway 读取 references/module-spec.md 与 Batch 14，检查当前 Elmos 实现，完成真实调试垂直切片、自动化测试和安全证据。
```

## Claude Code

```text
/elmos-debug-adapter-gateway 对照模块规格实现最高优先级缺口；必须使用固定 revision 和隔离沙箱，不得以 Mock Adapter 冒充完成。
```

## 验收调用

```text
使用 elmos-debug-adapter-gateway 只做验收：执行对应 AC、沙箱与权限测试，列出 adapter/runtime 不支持能力和可复现证据。
```
