interface:
  display_name: Elmos · 调试记录、检查点与运行回放
  short_description: 记录调试命令、暂停点、变量差异、副作用和环境检查点，并按能力等级实现会话回放、输入重放、检查点恢复和受支持运行时的反向调试。
  brand_color: '#0F766E'
  default_prompt: 使用 elmos-debug-record-replay 按模块规格实施、测试并提供可复现证据。
policy:
  allow_implicit_invocation: true
