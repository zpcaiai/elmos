# Native Runtime Lab Standard

Runtime evidence must bind source/target commands to immutable environment fingerprints: OS/arch, compiler/runtime version and flags, SDK, libraries, locale/timezone, DB/broker versions, container/VM image, fixture hash, secrets policy and hardware where relevant. Missing proprietary/native environments remain `not-run`.
