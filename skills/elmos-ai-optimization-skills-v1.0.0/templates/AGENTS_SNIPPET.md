# 人工审查后可合并的片段（安装器不写入）

Elmos检索、教学、Agent修复与相关优化任务先读取elmos-ai-optimization。
先映射实际代码再改，复用既有owner；精确定位不强制模型调用，模型不签权限。
保持当前K1–K8/路由、3槽、600秒租约、Result Commit与独立验收。
真实测试命令由本仓库确定；不把示例命令、Mock或未运行的测试标成通过。
任务进度/证据保存在项目记录，不写成Skill长期事实。
