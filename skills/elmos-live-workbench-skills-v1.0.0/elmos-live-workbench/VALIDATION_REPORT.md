# 验证报告：本地参考包，非已部署产品

生成时间（UTC）：2026-09-06T09:41:24.195506+00:00

## 实际执行结果

| 验证项 | 结果 | 实际范围 |
|---|---|---|
| 技能包静态验证 | 通过 | 28工作流、84工作流验收定义、12 Schema、10示例、12候选profile；依赖无环、批次无逆依赖 |
| Python单元/契约/安装测试 | **82项通过** | 源码Unicode坐标、摘要、固定租约、权限/epoch/fencing、证据绑定、Schema负测、安装撤回、发布门禁 |
| TypeScript宿主接口检查 | 通过 | strict + noEmit；仅类型契约，不是服务实现 |
| Python/JavaScript原生执行 | **12组输入全部一致** | 两个原创受信程序；包含合法、非法、阈值边界输入 |
| 变异对照 | **抓获错误** | 将>=10000改成>10000，输入10000产生错误total，被比较器检测 |
| 真实Python DAP实验 | **通过** | initialize/launch/断点/栈/基础变量/next/continue/terminated/disconnect |
| 完整P0发布门禁 | **BLOCKED（预期且正确）** | 18类部署级检查尚未执行，不能拿本地测试替代 |

DAP实测在源码第10行暂停，subtotal=10000、
discount=1000，单步推进到第11行。
这些值来自debugpy实际响应，不是伪造变量UI。测试只执行随包原创Python程序。

## 机器执行时间

- 单元测试进程：1.62185秒；unittest内部计时见原始日志。
- Python/JavaScript原生样例：8.087414秒。
- Python DAP实验：3.719983秒。
- TypeScript类型检查：0.899188秒。

上述是已执行测试的wall-clock；不是Elmos功能实现工期、任意仓库启动SLO或未来任务ETA。

## 明确未执行

没有部署Elmos源码工作台UI或后端；没有接入云端隔离provider；没有真实持续600秒预览；
没有真实云实例/依赖/凭据回收测试；没有四运行时族的生产资格认证；没有多租户安全实战验收；
没有修改实时Elmos仓库，也没有生成E3/E5生产证书。

所有12个适配器配置仍是candidate。基础600秒边界用注入时钟验证，不能宣传为10分钟真实运行。
两个cart样例是原创独立实现，不是从实际Elmos转换器产生；它们验证的是比较机制和有限输入行为。
Schema只能验证数据形状；示例签名/就绪/清理对象不是可信生产attestation。

## 工具环境

```json
{
  "python": "3.13.5",
  "node": "v22.16.0",
  "typescript": "Version 5.8.3",
  "debugpy": "1.8.20",
  "jsonschema": "4.26.0",
  "PyYAML": "6.0.3"
}
```

可复核原始材料位于 `reports/`。重新运行脚本会产生新报告，部署验收必须另建真实环境报告。
