---
id: 08-data-database-testing
name: Data & Database Testing
version: 1.1.0
category: testing
depends_on:
  - 05-test-model-dsl
---

# Data & Database Testing

## 目标

验证数据库 Schema、约束、事务、一致性、并发、查询、迁移、升级和回滚。

## 何时调用

当工作流进入 `08-data-database-testing` 对应阶段，或上游技能产物变化导致本阶段失效时调用。不得跳过依赖技能直接伪造输入。

## 输入契约

- DDL、ORM 模型、迁移脚本、查询和数据生命周期
- 业务不变量、隔离级别、容量与保留策略
- 合成/脱敏样本和生产规模分布模型

## 输出契约

- 数据库、迁移和数据质量测试
- 一致性/锁/死锁/索引/回滚诊断
- 迁移前后校验、数据 diff 和性能证据

## 执行步骤

1. 生成主键、外键、唯一、非空、检查约束和默认值测试。
2. 验证事务原子性、隔离、并发更新、死锁处理和重试。
3. 对关键查询生成正确性、执行计划和索引回退检查。
4. 在可恢复副本上执行升级、失败中断、继续和回滚测试。
5. 比较迁移前后行数、校验和、业务不变量和引用完整性。
6. 测试清理、归档、删除与审计策略。

## 不可违反的控制

- 禁止在生产数据库直接执行破坏性测试。
- 迁移测试必须使用与生产相同数据库版本/关键参数的隔离环境。
- 不得用行数相同替代语义一致性。
- 数据证据必须脱敏。

## 完成判定

- 迁移可重复执行或有明确幂等/防重复策略。
- 失败迁移不会留下未声明的半完成状态。
- 关键事务和并发不变量通过。
- 性能回退和缺失索引可被检测。

## 失败处理

- 输入缺失或 Schema 不合法：标记 `BLOCKED`，保留诊断，不得猜测为成功。
- 可恢复基础设施失败：保存检查点并按受限策略重试。
- 产品或策略失败：生成结构化缺陷/门禁结果，不得通过跳过或弱化规则绕过。
- 所有失败均写入证据清单和审计事件。

## 项目产出要求（v1.1.0）

- 本技能产生或修改的测试源、配置、数据、基线、报告、补丁或证据必须登记到 `ProjectOutputManifest`。
- 测试相关文件必须通过 `37-test-source-materialization` 写入目标生态原生目录；只存在于临时上下文不算完成。
- 任何文件变化都要更新 SHA-256、需求/用例引用和谱系；未登记文件不得进入最终 Bundle。
