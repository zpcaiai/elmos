# Certificate-bound Deployment Admission

Admission compares exact artifact, environment, policy, certificate, freshness and revocation. Webhook or policy-engine failure is fail-closed for production profiles.
