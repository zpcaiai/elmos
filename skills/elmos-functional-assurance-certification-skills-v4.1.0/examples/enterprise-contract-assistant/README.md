# Enterprise Contract Assistant Example

This example demonstrates a solution contract and target portfolio. It is intentionally `BLOCKED` for certification because it contains no native target evidence.

Targets:

- Dify prototype;
- LangGraph Python production workflow;
- Spring AI enterprise service;
- OpenClaw channel gateway.

The example tests package schemas and planning semantics only.
