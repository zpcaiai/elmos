# Verification State Model

States are monotonic only with fresh, exact-scope evidence. `specified`, `implemented`, `statically_validated`, `integration_tested`, `runtime_verified`, `independently_verified`, and `certified` are distinct. `blocked`, `failed`, `not_run`, `conditional`, and `revoked` are first-class outcomes.
