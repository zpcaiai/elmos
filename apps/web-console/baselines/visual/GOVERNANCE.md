# Generation visual baseline governance

No screenshot baseline is accepted automatically. A future baseline must bind the
route, viewport, browser, locale, theme, state, mask list, tolerance, approver,
source commit, and the reason for change. Failed screenshots must not update this
directory without an explicit owner decision.
