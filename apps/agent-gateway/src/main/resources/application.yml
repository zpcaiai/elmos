server:
  port: ${ELMOS_AGENT_GATEWAY_PORT:8083}
spring:
  application:
    name: elmos-agent-gateway
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
management:
  endpoints:
    web:
      exposure:
        include: health,info
