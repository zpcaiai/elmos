// Printed and compared from another file.  Java prints the constant's *name*,
// so an enum emitted as its ordinal would differ on the very first println.
public enum Op {
    ADD,
    SUB,
    KEEP
}
