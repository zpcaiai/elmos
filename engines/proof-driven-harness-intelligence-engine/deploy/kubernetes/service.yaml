apiVersion: v1
kind: Service
metadata:
  name: elmos-pdhi
  labels:
    app.kubernetes.io/name: elmos-pdhi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: elmos-pdhi
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
