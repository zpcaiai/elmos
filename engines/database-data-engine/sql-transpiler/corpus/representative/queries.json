{
  "schemaVersion": "1.0",
  "kind": "representative",
  "authority": "ELMOS synthetic representative workload; real customer workload NOT_RUN",
  "cases": [
    {
      "id": "REP-PG17-001",
      "sourceProfile": "postgresql-17.5",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-PG-001",
      "sourceProfile": "postgresql-18.4",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-MY-001",
      "sourceProfile": "mysql-8.4.10-lts",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-MS-001",
      "sourceProfile": "sqlserver-2022-cu26",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-OR-001",
      "sourceProfile": "oracle-26ai-ee",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-SQ-001",
      "sourceProfile": "sqlite-3.53.3",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= '2026-01-01' GROUP BY tenant_id, status ORDER BY tenant_id, status"
    },
    {
      "id": "REP-DU-001",
      "sourceProfile": "duckdb-1.5.4",
      "sql": "SELECT tenant_id, status, COUNT(*) AS order_count, SUM(total) AS gross_total FROM orders WHERE created_at >= CAST('2026-01-01' AS DATE) GROUP BY tenant_id, status ORDER BY tenant_id, status"
    }
  ]
}
