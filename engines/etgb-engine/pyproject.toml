[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "elmos-etgb-runtime"
version = "1.1.0"
description = "Repository-owned ETGB v1.1 execution, assurance and release-control runtime"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0", "jsonschema>=4.20", "cryptography>=42.0,<47.0"]

[project.scripts]
elmos-etgb = "elmos_etgb.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
