# Transformation boundary

The 72 exact directed transformations are materialized under `formal-campaign/routes/`. Each route binds canonical IR, source and target bytes, re-lifted IR, behavior traces, chunk maps, SMT input, solver output, and route evidence.

No reverse route, arbitrary customer source, browser runtime, or native-device support is implied.
