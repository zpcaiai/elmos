# Source snapshot boundary

The immutable generated source projects and their byte-bound manifests live under `formal-campaign/engine/profiles/`. Route-specific source bytes and re-lifted IR live under `formal-campaign/routes/`.

This local campaign does not claim an independently captured customer source snapshot. External source capture remains `NOT_RUN` and certification remains `NOT_CERTIFIED`.
