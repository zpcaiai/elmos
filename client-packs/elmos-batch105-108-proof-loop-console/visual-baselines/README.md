# Visual baselines

No approved visual baseline exists. Browser screenshots on failure are local diagnostics only and
must never be promoted automatically. Independent visual review remains `NOT_RUN`.
