# Source snapshots

The bounded source identity is recorded in `source-fingerprint/fingerprint.json`.
No customer repository, token, production payload, or mutable archive is stored here. A release
qualification must bind an immutable authorized snapshot before it may advance beyond experimental.
