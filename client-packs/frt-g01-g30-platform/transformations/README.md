# Transformations

FRT Skill contracts compile into the typed shared Runtime in `engines/frontend-client-engine/src/frt-runtime.ts`. Route-specific customer transformations require isolated external Runners and remain `NOT_RUN` in this pack.
