# Visual baselines

Status: `NOT_RUN` for independent approval. Candidate screenshots are emitted
only under Playwright test output. The external quality configuration reads the
`approved/` tree with `updateSnapshots=none`, zero masks and zero tolerance;
missing or changed baselines fail without creating or replacing approved files.
Promotion requires a signed `device_matrix` record from an independent verifier.
