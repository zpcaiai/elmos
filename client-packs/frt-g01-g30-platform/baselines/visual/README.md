# Governed visual baselines

Approved baselines are imported only after independent review and are bound by
the signed `device_matrix` evidence record. Candidate screenshots are written to
Playwright test output, never to this directory. Automatic baseline updates,
masks and tolerance widening are disabled.
