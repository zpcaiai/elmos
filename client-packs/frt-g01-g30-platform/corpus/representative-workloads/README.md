# Representative workloads

Synthetic local status: `PASSED` for a Vue 3 form workload.

The synthetic workload lives under `engines/frontend-client-engine/test/fixtures/frt/representative/` and covers typed `v-model` conversion. No authorized customer-derived or production-equivalent representative frontend repository was supplied; customer-representative evidence remains `NOT_RUN`.
