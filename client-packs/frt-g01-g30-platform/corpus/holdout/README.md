# Holdout corpus

Local engineering status: `PASSED` for the repository-isolated Vue 3 toggle fixture.

The holdout fixture lives under `engines/frontend-client-engine/test/fixtures/frt/holdout/`, has a content digest distinct from development/negative/representative fixtures, and is not used to implement the transformer. Independent external or physically separated holdout evidence remains `NOT_RUN`; this local fixture must not be relabeled as external certification evidence.
