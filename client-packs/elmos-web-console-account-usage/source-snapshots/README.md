# Source snapshot boundary

The exact repository-local source tuple is bound by the SHA-256 value in
`source-fingerprint/fingerprint.json`. No credential, customer usage row or
production billing record is stored in this pack.
