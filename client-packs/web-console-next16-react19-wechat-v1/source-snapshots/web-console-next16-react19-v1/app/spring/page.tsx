import type { Metadata } from "next";
import { SpringModernizationStudio } from "./SpringModernizationStudio";

export const metadata: Metadata = { title: "Spring 老项目翻新" };

export default function SpringModernizationPage() {
  return <SpringModernizationStudio />;
}
