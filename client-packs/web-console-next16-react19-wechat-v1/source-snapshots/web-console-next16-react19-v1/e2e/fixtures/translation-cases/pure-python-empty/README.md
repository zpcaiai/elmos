This fixture intentionally contains no `WU-*.json` behavior corpus. It verifies
that a zero-success conversion still produces a diagnostic conversion report
while the generated-code archive remains unavailable.
