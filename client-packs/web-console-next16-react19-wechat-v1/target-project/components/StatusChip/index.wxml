<text class="{{ (("status-chip status-" + status.toUpperCase().toLowerCase().replaceAll("_", "-")) + " ") + (compact ? "status-compact" : "") }}">
  <text />
  {{ ((status.toUpperCase() === "READY") ? "契约就绪" : ((status.toUpperCase() === "ENFORCED") ? "已强制" : ((status.toUpperCase() === "BLOCKED") ? "已阻断" : ((status.toUpperCase() === "NOT_RUN") ? "未运行" : ((status.toUpperCase() === "NOT_CERTIFIED") ? "未认证" : ((status.toUpperCase() === "NOT_CONFIGURED") ? "未配置" : ((status.toUpperCase() === "CONFIGURATION_REQUIRED") ? "待配置" : ((status.toUpperCase() === "EXPERIMENTAL") ? "实验性" : ((status.toUpperCase() === "LIMITED") ? "受限支持" : ((status.toUpperCase() === "RECOMMENDED") ? "推荐" : ((status.toUpperCase() === "OPTIONAL") ? "可选" : ((status.toUpperCase() === "CONDITIONAL") ? "条件选择" : ((status.toUpperCase() === "REVIEW") ? "待审阅" : ((status.toUpperCase() === "DRAFT") ? "草稿" : ((status.toUpperCase() === "SUPPORTED") ? "已支持" : ((status.toUpperCase() === "CERTIFIED") ? "已认证" : ((status.toUpperCase() === "PASSED") ? "已通过" : ((status.toUpperCase() === "PRECHECK") ? "非计费预检" : ((status.toUpperCase() === "FAILED") ? "已失败" : ((status.toUpperCase() === "MEASURED") ? "已计量" : ((status.toUpperCase() === "INDETERMINATE") ? "不可确定" : ((status.toUpperCase() === "LIVE_API") ? "实时 API" : ((status.toUpperCase() === "REPOSITORY_CONTRACT") ? "仓库契约" : ((status.toUpperCase() === "CONTRACT_READY") ? "契约就绪" : ((status.toUpperCase() === "INSTALLED") ? "已安装" : ((status.toUpperCase() === "ADAPTER_DECLARED") ? "适配器已声明" : null)))))))))))))))))))))))))) ?? status }}
</text>
