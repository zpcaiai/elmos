# Remaining formal and certification gaps

- Formally discharge every source-language analyzer lifting obligation.
- Add native corpora for short-circuit behavior, strings, floating-point special values, error paths, and evaluation order.
- Replace every TypeScript guard-abstraction PROVED_UNDER_ASSUMPTIONS, language-standard AXIOM, and narrow-width BOUNDED obligation with an emitted-runtime theorem or independently verified certificate.
- Execute the external `${ELMOS_REPO_ROOT}` native route replay separately; the packed launcher does evidence-integrity validation only.
- Run an independent verifier, representative customer workloads, production-equivalent regression, and external certification.
- Add separate exact packs for L1+ constructs, frameworks, databases, I/O, exceptions, aggregates, and concurrency.
