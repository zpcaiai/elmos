# Gap inventory

- Execute the exact local test manifest and bind raw output to the current
  target-artifact digest.
- Execute denied-action side-effect sentinels and the untouched holdout with
  separate executor and verifier identities.
- Run the PostgreSQL migration and concurrent two-tenant representative
  workload in an approved disposable environment.
- Run the bounded P0 mutation campaign; surviving critical mutants must be
  zero.
- Close the provider adapter, context compaction, parity harness, and SLO
  autotuning production call chains; complete the seven partial call chains.
- Obtain an independent P0 assurance review before any certification request.
- Keep provider, fleet, production, and external evidence `NOT_RUN` until it is
  actually authorized and executed.
