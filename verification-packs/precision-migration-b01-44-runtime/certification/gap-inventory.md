# Gap inventory

- Implement exact handlers for the 564 `INSTALLED` child Skills.
- Execute real native source/target builds for each exact route and version tuple.
- Run the frozen holdout corpus with an independent verifier; current status is `NOT_RUN`.
- Run representative customer-like language, framework, client, and database workloads; current status is `NOT_RUN`.
- Integrate production HSM/key custody, identity, revocation, retention, and operational monitoring.
- Resolve all P0 residual risks before requesting certification.
