# Repository layout

Developer Experience Packs live under `developer-experience-packs/<pack-key>`. Shared contracts, schemas, scripts, and tests live under `schemas/batch36`, `scripts/batch36`, `templates/batch36`, and `tests/batch36`. Product implementations should live in dedicated IDE extension, CLI, protocol, bot, local-agent, and developer-analytics modules.
