# Signing, SBOM, and provenance

Every release is immutable and content-addressed. Publish and install verify publisher identity, artifact signature, SBOM, license report, vulnerability state, reproducible build provenance, and revocation. Mutable tags are discovery aliases only.
