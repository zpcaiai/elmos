# SDK, ABI, and compatibility

Version product, protocol, SDK, schemas, packs, and extensions independently. Support claims require automated N-1/N/N+1 or LTS contract tests. Private APIs are never compatibility promises. Deprecations require migration tooling and an exit date.
