# Sandbox security policy

Deny by default. Declare filesystem, network, process, secret, model, repository, runner, and evidence permissions. No wildcard egress, host paths, privileged process, cross-tenant references, or persistent credentials. Cleanup must terminate processes, remove workspace, and revoke credentials.
