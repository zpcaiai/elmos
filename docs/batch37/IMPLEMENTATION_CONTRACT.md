# Batch 37 implementation contract

All extensions use typed manifests and versioned SDK contracts. Core product internals are not extension APIs. Every execution is bound to an exact tenant, product version, release digest, permission decision, sandbox policy, publisher, and evidence envelope.
