# Cache and transfer policy

Cache keys include complete source, dependency, toolchain, profile, policy, environment, and generator inputs. Artifacts are immutable, verified, provenance-linked, and tenant/trust scoped. Transfers are chunked, encrypted, resumable, region-policy checked, atomic, and cleaned after TTL.
