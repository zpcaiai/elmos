# Batch 38–45 strict suite implementation contract

- Test executable behavior and material side effects, never file presence or prose.
- Preserve the 400 exact case IDs, 30 owners, product Skills 1325–1496, and the source Batch boundaries.
- Use real systems where a case requires them; document any approved equivalent without representing it as production evidence.
- Store raw evidence before summaries and keep development, holdout, and representative corpora physically and logically independent.
- Bind every passed result to exact case, catalog, artifact, environment, raw evidence, authorization, identities, replay, and timestamps.
- Keep execution, independent verification, customer acceptance, third-party review, and final certification authority separate.
- Only `scripts/test-suite-b38-45/run_strict_gate.py` may derive the suite decision. Status fields in templates or summaries have no authority.
- Local validation must not mutate the 400 checked-in `not-run` results. External or production-equivalent evidence remains `NOT_RUN` until actually executed and authorized.
