# python to java

Compiler-backed directed route for the exact `typed-pure-function-v1` profile. The reverse direction is a separate route. Native parsing, target compilation, and three local behavior corpora pass; independent external certification remains `NOT_RUN`.
